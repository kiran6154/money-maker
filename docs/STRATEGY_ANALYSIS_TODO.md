# Strategy analysis — TODO & open questions

**This is the single backlog for everything strategy-related.** Every gap, open
question, suspected mis-signal, parameter worth revisiting, or analysis
follow-up that concerns a `Strategy` bean, its rules, or the signals it emits is
recorded here — **not** in [`GAPS.md`](GAPS.md), and not as a `// TODO` comment
in the tree.

> **Why separate from `GAPS.md`.** `GAPS.md` is infrastructure debt — schedulers,
> wiring, schema, delivery. The items here change **what trades get taken**, so
> they need a different bar before anything is acted on: each one wants a
> measured impact from a backtest run, not just a code fix, and each one is a
> live-behaviour change that must be confirmed with the user first. Mixing the
> two buries the trading questions in a list of plumbing chores.

For what the strategies currently *do*, see [`STRATEGIES.md`](STRATEGIES.md).
This page is only what is unresolved about them.

---

## Working rules for this file

1. **Every strategy gap lands here first.** If you notice it while doing
   something else, write the entry before you move on — a strategy question left
   in a commit message or a code comment is lost.
2. **Never state an impact you have not measured.** Write `Impact | Unquantified`
   and say what would measure it. A guessed trade-count delta is worse than an
   admitted unknown, because the next reader will act on it.
3. **Never change strategy behaviour to close an entry without asking.** These
   are live trading rules. Propose, get confirmation, then re-run the same
   backtest window before and after and record both numbers in the entry.
4. **Resolved entries stay**, moved to the bottom with the date and what the
   measurement showed. The rejected options matter as much as the chosen one.

Entry format matches `GAPS.md`: **what** / **where** / **why** / **impact** /
**fix sketch** / **effort**. Effort legend: **S** < 1 hour, single file · **M**
half-day, 2-4 files + doc · **L** multi-day, schema or cross-cutting.

Ids are `S<n>` so they never collide with `GAPS.md` numbering.

---

## Open

### S1. `Strategy2`'s SMA-20 slope filter is inert when the slope is unknown — **PARKED 2026-08-30**

| | |
|---|---|
| Where | [`CommonRules.isSma20SlopeUp`](../src/main/java/com/moneymaker/strategy/rules/CommonRules.java), consumed as the `sma20SlopeNotUp` required rule in [`Strategy2.sellRulesFor`](../src/main/java/com/moneymaker/strategy/Strategy2.java) |
| Why | The predicate needs two same-day SMA-20 values to compute a slope. When it cannot get them it returns `false` — "not sloping up" — and the SELL entry proceeds unfiltered, i.e. `Strategy2` degrades to `Strategy1` for that candle. **In practice one case dominates:** the day's first *settled* bar, whose predecessor in the series belongs to the previous session — an overnight gap, not a slope (`previousSameDayCandle` returns null; `SmaTrendCalculator` resets at the same boundary for the same reason). That recurs every session, on every leg, on every timeframe, and it lands on exactly the bar where a gap-up open would otherwise be filtered. Note the stale-bar guard added 2026-08-30 ([S3](#s3-stale-bar-guard-asof--resolved-2026-08-30)) does **not** remove this: it skips bars from an *earlier* session, so the first bar it lets through is precisely the one with no same-day predecessor. The other two cases are near-theoretical and should not drive the fix: SMA-20 warm-up only bites if the *whole* fetched series is under 20 candles (`SMAIndicatorImpl` returns null and stamps nothing only when `period > size`; inside the series it writes a partial-window value from the first candle), and the lookback window spans weeks — and a missing SMA-20 column requires a timeframe that does not register period 20 in `allTimeFrameMap`, which none currently do. Either way it is silent: the `[tick]` log shows `sma20SlopeNotUp` passing, indistinguishable from a genuinely flat or falling SMA. |
| Impact | **Unquantified.** The measurement is narrower than it first looked: count `Strategy2` SELL entries whose candle is the first settled bar of its session, per timeframe. Upper bound is one bar per leg per timeframe per day — so on a 5-minute series with ~75 bars it is ~1.3% of evaluated bars, but those bars are not average bars: they carry the overnight gap, which is when a rising SMA-20 is most likely to be exactly what the filter was meant to catch. Compare realised P&L on that subset against the rest before concluding it is negligible. |
| Count measured 2026-08-31 (the [S4](#s4-the-036-exit-bracket--sl-ceiling--trailing-ladder--is-shipped-but-unmeasured) Run B ledger, 2024-01-01 → 2024-01-31) | **2 of 8 `Strategy2` SELL entries (25%) fell on the first settled bar of their session — 1 of 5 on `5min`, 1 of 3 on `15min`.** `entry_time` is the decision candle's timestamp (`TradeSignal.signalTime = lastCandle.getTimestamp()`), and with a 09:15 session open the first settled bar is stamped 09:15 on every timeframe, so the subset is exactly the entries stamped 09:15. Both landed on the same session, 2024-01-25: id 1386 `5min/SMA200` 21200 CE, exit `TRAIL_SL` **−23.30**; id 1388 `15min/SMA50` 21400 CE, exit `TARGET` **+45.35**. (Run A, the 035 arm, had 1 of 6 — id 1372, the same 21200 CE, exiting `TARGET +45.20`.) **The count exists; the P&L comparison this entry asks for does not.** Two observations pointing opposite ways on one session cannot say whether the exemption helps or hurts, and neither can they be separated into gap-up and gap-down as the caveat below requires. **Status unchanged — still parked; this is a count, not a verdict.** A window with many more `Strategy2` sessions is needed before the comparison means anything. |
| Decision (Rule 0) | **2026-08-30 — no change for now. Current behaviour stands: unknown slope allows the entry.** The direction to try *when* it is revisited is **treat the overnight gap as the slope** — i.e. let the predicate use the previous session's last SMA-20 as the comparison point instead of returning "unknown", so the day's first settled bar gets a real up/down answer rather than an exemption. Signed off as a deliberate park, not an oversight: the behaviour is documented in [STRATEGIES.md](STRATEGIES.md#strategy-2--the-sma-20-slope-filter) and in the `isSma20SlopeUp` Javadoc, so nothing is silently wrong in the meantime. |
| Caveat to carry into that attempt | Treating the gap as the slope answers a *different* question than the rule was written to ask, and the asymmetry matters: on a **gap-down** open the overnight move reads as "sloping down", so the filter would **permit** a sell it currently exempts — the opposite of tightening. Whoever picks this up should measure gap-up and gap-down opens separately; a single blended trade count will hide it. |
| If revisited, first | Make the two cases distinguishable before changing anything: have the rule report "slope unknown" separately from "slope down/flat" in its `TradeRule.named(...)` label, so a replay can be counted. Fetching more history is *not* a lever here — `previousSameDayCandle` rejects a cross-day predecessor by construction, not for want of data. The rejected alternatives were: fail closed (suppress until a same-day slope exists), and formally document the first-bar exemption as intended. |
| Effort | **S** — the change itself is a single predicate in `CommonRules`. The cost is the before/after measurement and the sign-off, not the code. |
| Priority | **Deferred** — parked by the user 2026-08-30, worth trying later. Not blocking; revisit alongside the next `Strategy2` backtest, when a before/after window is being run anyway. |

> Nothing here is a defect. The code matches its documentation — the Javadoc on
> `isSma20SlopeUp` and the STRATEGIES.md section both state the "unknown ⇒ allow"
> rule explicitly. This entry stays open only because a *better* default may
> exist, and it is parked until someone measures whether it does.

---

### S4. The 036 exit bracket — SL ceiling + trailing ladder — is shipped but unmeasured

| | |
|---|---|
| Where | [`OrderService.capStopLoss` / `trailLadderAtEntry`](../src/main/java/com/moneymaker/order/service/OrderService.java), [`PositionService.applyTrail` / `thresholdBreach`](../src/main/java/com/moneymaker/position/service/PositionService.java), [`TrailLadder`](../src/main/java/com/moneymaker/util/TrailLadder.java), changeset `036_add_trailing_stop_loss.xml`. Config: `trade_config.max_sl_points`, `trade_config.trail_ladder`. |
| Why | Requested by the user 2026-08-30: *"target can be more, sl should be less. keep max sl as 60 points … whichever is lower for sl. profit needs to be tailed, say after 25 points profit move sl to 2 points. then after 50 move sl to 25 points so on."* Built as specified. It changes **where open trades exit**, on both sides: the ceiling tightens the stop at the top of the premium band, and the ladder converts realised excursion into a floor. Every rung and the ceiling are config columns — nothing about the ladder is hardcoded — and existing rows were seeded with `60` / `25:2,50:25,75:50,100:75` by 036, so **this is live for every existing config, not opt-in**. |
| Correction (2026-08-30) | **The feature was inert when it first landed and for some hours after.** `max_sl_points` and `trail_ladder` were on the entity and in the DB but absent from `TradeConfigRepository.fetchCombinedByTradingDate`'s SELECT list and from `TradeConfigScheduler.mapToTradeConfig`, so every DTO in `SharedData.combinedDto` carried null for both: `capStopLoss` was a no-op and no trade ever trailed, in live and backtest alike, with nothing logged. Found by a peer session, verified, and fixed in the same query + mapper. **Any backtest run before that fix measures the absence of the feature, not the feature** — discard those numbers; a zero `TRAIL_SL` count from such a run is an artifact. Now guarded by `TradeConfigCombinedQueryContractTest`, which fails if the SELECT list drifts from the mapper ordinals again. |
| Impact | **Measured 2026-08-31 on 2024-01-01 → 2024-01-31, `HISTORICAL_ICICI`.** One variable: `max_sl_points` / `trail_ladder` NULL (Run A, 035) vs the seeded `60` / `25:2,50:25,75:50,100:75` (Run B, 036). Same 18 configs, same 1047 ticks, app restarted between runs, ledger cleared each time; `trade_config` count and max id unchanged throughout, so the detector wrote nothing and the runs are comparable. P&L below is **per share** (`trade_order.profit` is `perShareProfit`, no lot multiplier; every row carries `quantity = 75`), charges not modelled.<br><br>| | Run A (035) | Run B (036) |<br>|---|---:|---:|<br>| trades | 12 | 16 |<br>| SIGNAL / TARGET / STOP_LOSS / TRAIL_SL / FORCE_CLOSE | 8 / 4 / 0 / 0 / 0 | 5 / 2 / 3 / 6 / 0 |<br>| win rate | 58.3% | 43.8% |<br>| mean P&L per trade | +9.7417 | −8.8188 |<br>| **total P&L** | **+116.90** | **−141.10** |<br><br>**Δ −258.00 per share** (−19,350 at 75/lot). All 12 of A's trades recur in B on the same (config, strategy, leg, entry_time); B adds 4 that 035 never took. Decomposition: matched-trade effect **−226.10**, newly-admitted-trade effect **−31.90**. **(a) The ceiling** bound on 4 of the 5 configs that traded (62.53 / 77.69 / 100.55 / 63.42 → 60.00; only config 1080's 27.65 was already under it), produced all 3 `STOP_LOSS` exits, and cost −6.70 on the one matched trade it changed. **(b) The ladder** produced 6 `TRAIL_SL` exits with a total `peak_profit` giveback of **219.50** (mean 36.58/trade); it gained +3.80 twice and cost −68.50 twice and −45.00 twice, every loss by pre-empting a `TARGET` that 035 went on to reach. Full tables, per-trade diff and artifacts: [`analysis/s4-measurement/RESULTS.md`](../analysis/s4-measurement/RESULTS.md). |
| Two premises in this entry that the measurement contradicted (2026-08-31) | **(1) "The ceiling only binds above a ~200-point entry with `sl_pct = 0.30`" — no.** Because [S6](#s6-target_pct--sl_pct-never-reach-the-running-pipeline--the-percentage-bracket-has-never-actually-run) is still unwired, `sl_pct` never reaches the pipeline and `bracketAtEntry` falls back to the absolute `trade_config.stop_loss` column; the ceiling is compared against *that*, so it binds wherever the absolute stop exceeds 60 — 8 of the 18 window configs. Visible on the ledger: order 1371 opened at 229.80 with `target_at_entry = 42.28`, the config's absolute `target`, not 229.80 × 0.20. **(2) "The ladder can only exit a trade *green*" — no.** 4 of the 6 `TRAIL_SL` exits closed red (−23.30, −23.30, −2.05, −2.05). The floor is only tested on 5-minute monitor ticks, so a bar that travels far enough between two ticks passes straight through it and `thresholdBreach` still labels the exit `TRAIL_SL` because `pnl <= trailSl` holds from far below: order 1385 was at peak +35.30 on the 09:20 tick and −23.30 on the 09:25 tick. This is a separate property from the dead zone below and is the largest single contributor to the −258.00. |
| Caveats on the numbers | **The bracket changed the trade *count*, not only the exits** — with `no_of_parrellel_trades = 1` an earlier exit frees the slot, so 036 admitted 4 entries 035 was still holding the slot against (cap enforcement itself was correct: the Run B log shows 1379 closing before 1381 opens). **The sample is small and not independent** — 12 and 16 rows, every config tagged `strategy_ids = '1,2'` so the rows come in near-duplicate Strategy1/Strategy2 pairs (~8 independent trades in B), and −137.00 of the swing is one session, 2024-01-25. Enough to say the ladder as configured hurt on this window; **not** enough to size the effect. A second window is wanted before anything is concluded. |
| Superseded by S6's wiring (2026-08-31) — read before acting on the numbers above | [S6](#s6-target_pct--sl_pct-never-reach-the-running-pipeline--the-percentage-bracket-has-never-actually-run) landed hours after this measurement and **changed the world it was taken in**. With `sl_pct` now reaching the pipeline the stop scales to premium, so the 60-point ceiling binds on **24% of entries instead of 88%**, and `TRAIL_SL` exits fell 6 → 4 on the same window. The ladder result above still stands (the ladder is unchanged and still cost the window), but **the ceiling half of this entry is measured against a bracket the system no longer uses** — do not quote "bound on 4 of the 5 configs that traded" as current. The dead zone below also needs re-reading: with targets now at 20% of premium, an 80-point leg targets 16 against a 25-point first rung, which is the same inertness but arrived at for a different reason. A fresh S4 pair on top of the wired bracket is what would settle the ceiling question. |
| Known dead zone | The first rung must be reachable before the target or the ladder never arms. With `target_pct = 0.20` an 80-point leg targets 16, below the 25-point first rung, so the ladder is **inert below a ~125-point entry** — i.e. on a meaningful slice of the standing 80-250 band. Whether the rungs should be premium-relative (like `target_pct`) rather than absolute points is the obvious follow-up, and is exactly what the measurement above would settle. Not changed speculatively: the user specified points. |
| Fix sketch | **The before/after window is now run and recorded above (2026-08-31); nothing has been changed as a result, and nothing should be until the user decides.** Three questions are now on the table, and they are separable: (1) the dead zone below — should the rungs be premium-relative rather than absolute points; (2) whether the first rung `25:2` is simply too tight, since every trailed exit on the window fired off it and none reached `50:25`; (3) whether a `TRAIL_SL` that gaps through its floor and closes red should be labelled `TRAIL_SL` at all, or is a `STOP_LOSS` wearing the wrong name — a reporting question, not a trading one, but it decides whether this measurement's exit mix reads correctly. Per working rule 3 none of these is actioned without sign-off and its own before/after pair. |
| Decision (Rule 0) | **2026-08-31 — signed off by the user: the ladder stays, and the floor-leak is a backtest modelling artifact, not a design flaw.** The user's rationale, verbatim in substance: *in live, orders are placed as price makes new values — once profit reaches 25, an actual SL order at +2 rests at the broker — hence tick granularity does not matter in backtest.* The backtest must therefore model the floor as a resting order: when a monitored bar's adverse extreme crosses the latched floor, the trade exits **at the floor price** (profit = the locked rung), not at whatever the next 5-minute close happens to be. This eliminates the red `TRAIL_SL` exits by construction (a floor at +2 fills at +2). Re-measured with a fresh ladder pair (seeded vs NULL) on the wired pct bracket after the change — numbers to be recorded here. The dead-zone and rung-tuning questions (fix-sketch items 1–2) remain open and separately gated. |
| Re-measured under the resting-order model (2026-08-31, [`analysis/s4b-measurement/RESULTS.md`](../analysis/s4b-measurement/RESULTS.md)) | Same window, S8 fix active in both arms, one variable (ladder+ceiling seeded vs NULL): **ladder ON 19 trades +80.94, ladder OFF 15 trades +85.29** per share. The corrected ladder costs **4.35 points of total P&L** in exchange for a much steadier profile — 15/4 win/loss vs 9/6, every trailing exit green (+12.00 combined vs the old model's 219.50 giveback), STOP_LOSS exits identical in both arms. **The ladder is no longer a defect; keep-vs-drop is now a risk-preference call for the user.** Note both arms flipped positive versus every earlier measurement — that is [S8](#s8-a-strike-that-leaves-the-configs-atm-window-keeps-trading-on-its-last-cached-bar-for-the-rest-of-the-day)'s doing, not the bracket's. Caveat [S12](#s12-the-backtest-now-assumes-live-places-resting-sl-orders--live-does-not-yet-do-that): live must not run the ladder until resting SL orders exist there. |
| Full-2024 evidence on the rung-tuning question (2026-08-31) | The year-long replay (161 trades, resting-order model, run `bt-2024-01-01_2024-12-31-1788159770820`) adds volume to fix-sketch item (2): 21 `TRAIL_SL` exits totalling **+65.00** per share (avg +3.10 -- all green, consistent with the resting-order model). **20 trades (12.4%) reached `peak_profit >= target_at_entry` yet exited otherwise**: 9 of them via `TRAIL_SL` at the first rung's +2 lock, 5 reversed all the way to `STOP_LOSS`, the rest via SIGNAL. Mean giveback (`peak_profit - profit`) across all 161 trades is 23.08 points. On the protective side the ladder does work once armed: `trail_sl_at` was set on 84/161 trades and only 2 of those ended `STOP_LOSS` (-66.86 combined). Input to the still-open rung-tuning decision, not a verdict -- per working rule 3 nothing is retuned without sign-off and a paired run. |
| Effort | **M** — the measurement itself is done. Any change that follows is **S** for the code plus another paired run. |
| Priority | ~~High~~ **Decision available** — the corrected pair is measured; awaiting the user's keep-vs-drop preference. Dead-zone and rung-tuning questions remain open. |

---

### S6. `target_pct` / `sl_pct` never reach the running pipeline — the percentage bracket has never actually run

| | |
|---|---|
| Where | [`TradeConfigRepository.fetchCombinedByTradingDate`](../src/main/java/com/moneymaker/repository/TradeConfigRepository.java) SELECT list and [`TradeConfigScheduler.mapToTradeConfig`](../src/main/java/com/moneymaker/scheduler/TradeConfigScheduler.java). Consumed (or rather, not) by `OrderService.bracketAtEntry`. |
| Why | Same class of defect as the 036 one recorded in [S4](#s4-the-036-exit-bracket--sl-ceiling--trailing-ladder--is-shipped-but-unmeasured), found while fixing it, but **older and wider**. Changeset 027 added `target_pct` / `sl_pct` to `trade_config` and `OrderService.bracketAtEntry` prefers them over the absolute columns — but neither column is selected by the native combined query or set by the positional mapper. So `getTargetPct()` / `getSlPct()` are null on every DTO the pipeline ever sees, `bracketAtEntry` always takes its absolute-column fallback, and **every trade since 027 has exited on `trade_config.target` / `stop_loss` points, not on the percentage**. The percentage is persisted, is visible in the admin UI, is copied onto generated configs by `EodDowntrendDetectionService`, and is documented as the bracket that decides exits — it just never arrives. |
| Impact (was unquantified; **measured 2026-08-31**) | Paired run on 2024-01-01 → 2024-01-31, `HISTORICAL_ICICI`, one jar for both arms (`6ed316b` + the wiring change), 85 tests green before either arm. One variable, toggled by config: **Arm A** `target_pct` / `sl_pct` NULL (the absolute-column fallback every trade has taken since 027); **Arm B** the seeded `0.20` / `0.30`. The 036 bracket (`max_sl_points = 60`, `trail_ladder`) stayed **seeded in both arms**, so this isolates S6 from [S4](#s4-the-036-exit-bracket--sl-ceiling--trailing-ladder--is-shipped-but-unmeasured). `trade_config` count and max id unchanged throughout; both arms replayed 1047 ticks. P&L is **per share**, `quantity = 75`, charges not modelled.<br><br>| | Arm A (absolute) | Arm B (pct) |<br>|---|---:|---:|<br>| trades | 16 | 21 |<br>| **TARGET hit-rate** | **12.5%** (2/16) | **33.3%** (7/21) |<br>| SIGNAL / TARGET / STOP_LOSS / TRAIL_SL / FORCE_CLOSE | 5 / 2 / 3 / 6 / 0 | 5 / 7 / 5 / 4 / 0 |<br>| win rate | 43.8% | 42.9% |<br>| mean P&L per trade | −8.8188 | −6.0667 |<br>| **total P&L** | **−141.10** | **−127.40** |<br><br>**Δ +13.70 per share** (+1,027.50 at 75/lot). The percentage bracket does what 027 predicted — it converts exits into `TARGET` hits, +20.8 pp — but **both arms lose money on this window**, and the net gain does not come from better exits: on the 16 trades both arms took, the pct bracket was **−10.25 worse**; the whole +13.70 comes from **5 entries the pct bracket admitted** (+23.95) via the same `no_of_parrellel_trades = 1` slot-freeing mechanism S4 hit. Two of those five are one independent trade pair worth +45.80 each. Full tables and per-trade diff: [`analysis/s6-measurement/RESULTS.md`](../analysis/s6-measurement/RESULTS.md). |
| Ceiling interaction — this **inverts** S4's ceiling finding (2026-08-31) | Rows opening at the 60-point ceiling: **Arm A 14/16 (88%) → Arm B 5/21 (24%)**. Under the absolute columns the detector's ATR-derived stops were mostly above 60, so the ceiling bound almost everywhere; under `sl_pct = 0.30` the stop scales to premium and only an entry above ~200 clears 60 (only config 1086, entry 229.80 → raw 68.94 → capped). **That is exactly the binding condition S4's entry originally asserted and which was false when written — it is true now, because of this change.** So S4's measured "ceiling bound on 4 of 5 configs" describes a configuration that no longer exists, and whatever work the ceiling was doing it is now doing on a quarter as many trades. Input to the still-open S4 decision; not settled here. Related: `TRAIL_SL` fell 6 → 4 for the same reason — a 20%-of-premium target is reached before the ladder's first rung on legs where the old absolute target was unreachable (22000 PE on 01-16, `TRAIL_SL +1.95` → `TARGET +21.00`, twice). |
| Note on 027's own numbers | The measured table in changeset 027 came from an offline sweep over the imported option series, not from the live pipeline — so it is not invalidated by this. What is invalidated is any assumption that the running system has been trading that bracket. **The 2026-08-31 run does not reproduce 027's levels and should not be expected to:** 027 reported 45.4% → 53.9%, this run 12.5% → 33.3% — same direction, larger delta, far lower level. Three reasons, all structural: 027 had no 036 bracket (here `TRAIL_SL` competes with `TARGET` in both arms); 027 replayed *every* 5-minute candle as a hypothetical entry with first touch on the bar's high/low, whereas the pipeline takes 21 trades in a month and tests thresholds on the monitor tick's close; and 027's "before" was a flat 30/30, while Arm A's is the detector's per-config ATR values ranging 18.44–67.03. |
| Decision (Rule 0) | **2026-08-31 — signed off by the user: wire both columns and keep the percentage bracket live**, with the paired measurement recorded here (same Jan-2024 window, before/after). Execution is queued strictly *behind* the S4 measurement pair so the two changes never share a run. |
| Fix sketch | **Landed 2026-08-31, exactly as sketched.** `tc.target_pct, tc.sl_pct` appended at indices 22-23; `setTargetPct` / `setSlPct` added to `mapToTradeConfig`; `mapToInstrument` bumped 22 → 24 and `mapToInstrumentDetails` 27 → 29; `EXPECTED_COLUMNS` and the block-boundary constants extended, plus a new `percentageBracketReachesTheConfig` assertion so this pair can never go inert again the way 036's did. The percentage bracket is **live and staying live** — that is the signed-off outcome, not a temporary measurement state. |
| Why this entry stays Open | The wiring is done and the numbers are recorded, but two things it surfaced are undecided and both belong to the user: **(1)** the bracket improves a **losing** month (−141.10 → −127.40) and does so via admitted entries rather than better exits, so "keep the percentage bracket" is settled while "is `0.20` / `0.30` the right pair" is not — nobody has swept the values; **(2)** it left the 036 ceiling binding on a quarter as many trades, which changes the premise of the open S4 decision. Neither is actioned without its own sign-off and before/after pair. |
| Effort | **S** for the code; **M** including the paired measurement and sign-off. |
| Priority | **High** — ~~it silently contradicts what `ORDERS_AND_POSITIONS.md`, `EOD_DOWNTREND.md` and the admin UI all say is happening~~ resolved 2026-08-31: the pipeline now does what those three documents say. Remaining priority is the open tuning question above. |


### S5. Session-window constants are hardcoded while `app.market.*` already exists — **RESOLVED 2026-08-31**

> Moved to [Resolved](#s5-session-window-constants-are-hardcoded-while-appmarket-already-exists--resolved-2026-08-31) with the paired-run numbers.

---

### S14. Should the same-side parallel cap span a strategy *family* rather than one (config, strategy)?

| | |
|---|---|
| Where | The `max_parallel_per_side` gate in [`OrderService`](../src/main/java/com/moneymaker/order/service/OrderService.java) (changeset 038, user-specified 2026-08-31), keyed — like every other cap — on `(tradeConfigId, strategyId)`. |
| Why | The user's framing was *"if in **group of strategy** say sma group, if a trade exists in CE side … another entry should not be taken."* The shipped gate caps per `(config, strategy)`, which fixes the observed case (orders 1941/1942 were the same strategy) — but two strategies tagged on one config can still each hold one CE trade simultaneously, which is the *intended* consequence of the changeset-031 ledger identity. If the cap should instead span every strategy in a family (e.g. SMA = strategies 1+2), that needs the strategy family/parent concept (`strategy_definition`, planned with the versioning work) and a deliberate partial rollback of the per-strategy ledger independence. |
| Impact | **Unquantified.** Count, on a fixed window run with both strategies, ticks where strategies 1 and 2 hold same-side OPEN trades concurrently and a family-level cap would have suppressed the later entry; split P&L of those trades. |
| Fix sketch | Once `strategy_definition` exists: a `cap_scope` choosing `PER_STRATEGY` vs `PER_FAMILY`; the gate then counts OPEN rows across the family's strategy ids. Rule 0: needs sign-off and a paired run. |
| Effort | **S** on top of the family table; blocked on that table existing. |
| Priority | _TBD — awaiting the strategy-family/versioning build._ |

---

### S12. The backtest now assumes live places resting SL orders — live does not yet do that

| | |
|---|---|
| Where | [`PositionService`](../src/main/java/com/moneymaker/position/service/PositionService.java) (the resting-order stop model from the S4 decision) versus the live path: live monitoring is `PositionScheduler` polling LTP every 5 minutes and exiting by market order via `OrderService.closeManually` — no SL order ever rests at the broker. |
| Why | The 2026-08-31 S4 sign-off ("*in live, once profit is 25 an SL order at 2 is placed — hence granularity does not matter in backtest*") justified modelling floors as resting orders in backtest: breach on the bar's adverse extreme, fill at the floor price. That is now how the backtest exits. **But live code today is still the 5-minute poll** — so the backtest models a live behaviour that does not exist yet. Until live actually places (and re-places, on each rung change) a broker SL order, live fills will be worse than backtest fills on fast moves: the exact gap the model assumed away. |
| Impact | **Unquantified.** Bounded by the same giveback the S4 measurement saw from the tick-granularity side (219.50 points of peak giveback on one month), since a live poll has the same granularity the old backtest had. Measuring it properly needs live fills to compare against, which don't exist yet. |
| Fix sketch | Implement resting SL orders in the live path: place an SL order at the floor when a rung arms, modify it on each rung change, cancel it on any other exit. Depends on Zerodha order placement being able to place/modify SL order types, and interacts with `exit_broker_order_id` bookkeeping. Rule 0: needs its own sign-off; also an infra design (order-type support per broker). |
| Effort | **M–L** — broker order-type work plus lifecycle bookkeeping. |
| Priority | **Should precede live trading with the ladder enabled** — filed 2026-08-31 the moment the backtest model landed, so the parity gap is on record. |

---

### S7. Signal flip-flop consumes the daily trade cap — **RESOLVED 2026-08-31**

> Moved to [Resolved](#s7-signal-flip-flop-consumes-the-daily-trade-cap--resolved-2026-08-31): zero occurrences on the post-fix month; closed by the user.

---

### S8. A strike that leaves the config's ATM window keeps trading on its last cached bar for the rest of the day

| | |
|---|---|
| Where | [`SharedData.strikeMarketDataByInstrumentAndInterval`](../src/main/java/com/moneymaker/shared/data/SharedData.java), written per tick by [`AnalysisScheduler.fetchAndShareStrikeMarketData`](../src/main/java/com/moneymaker/scheduler/AnalysisScheduler.java) and read by [`AbstractSmaCrossStrategy.execute`](../src/main/java/com/moneymaker/strategy/AbstractSmaCrossStrategy.java); cleared only at day end by [`BacktestAnalysisService`](../src/main/java/com/moneymaker/backtesting/BacktestAnalysisService.java). |
| Why | The map is `put` per tick for the strikes the config's ATM window currently selects, and is **cleared once per backtest day, not per tick**. When spot moves far enough that a strike drops out of the `itm_depth` / `otm_depth` window, its entry is never refreshed and never evicted — so the strategy keeps re-evaluating that leg against a **frozen candle list** for the rest of the session. The stale-bar guard from [S3](#s3-stale-bar-guard-asof--resolved-2026-08-30) does not catch it: that guard rejects a bar from an *earlier session*, and this bar is from the same day. The day-end wipe's own comment already names the failure mode ("leftover entries in SharedData's strike maps cause Strategy1 to evaluate stale strikes") but closes it only at the day boundary. |
| Observed 2026-08-31 (the [S4](#s4-the-036-exit-bracket--sl-ceiling--trailing-ladder--is-shipped-but-unmeasured) Run B ledger + log, 2024-01-16) | Orders 1381 / 1382 opened on 22200 PE at **142.80 — the close of the bar stamped 11:55 — on a tick after 12:45**, by which time that leg was actually trading near 191. The identical `[signal]` line (`time=2024-01-16T11:55`, same open, close and `sma50=156.166`) was re-emitted on ~20 consecutive ticks, i.e. the leg's series stopped advancing while the strategy kept acting on it. The raw `historical_option_candles` rows for that leg are continuous every 5 minutes across the whole period, so this is not a data gap. Both trades stopped out at **−61.30**. |
| Impact | **Unquantified.** The one instance measured is worth −122.60 per share across the two strategy rows, but a single occurrence on one session says nothing about frequency. What would measure it: replay a window with the `[index]` DEBUG line on and count, per tick, the strike keys the strategy evaluates that were **not** written by that tick's fetch — then split realised P&L by whether the entry came from a fresh or a stale key. Note this is **not** caused by changeset 036: it exists identically under 035, which merely kept the `no_of_parrellel_trades = 1` slot occupied so the stale signal never got a chance to fill. The S4 run exposed it; it did not create it. |
| Fix sketch | Nothing until measured and signed off — it changes which entries fire, so it is a Rule 0 change. The obvious lever is to evict or ignore any strike key not written by the current tick (clear the map per tick rather than per day, or stamp each entry with its tick and have `keyMatches` reject a stale one). Both change the entry set, and the second is the safer shape because it fails visibly. Worth checking the live path too: `AnalysisScheduler` populates the same static map on the cron, and nothing clears it between live ticks at all. |
| Decision (Rule 0) | **2026-08-31 — signed off by the user: fix it.** Taking the safer shape from the sketch: each cache entry is stamped with the tick that wrote it (`SharedData.strikeMarketDataTick`), and the strategy skips (with a visible log line) any key not written by the tick being evaluated — in live and backtest alike. |
| Measured (2026-08-31) | **The bug was constant, not occasional: 8,296 stale-key skips in one month's replay** once the guard landed. Jointly with the resting-order exit model (one build — entry-set differences are attributable to this fix alone, since only it changes which entries fire), the window flipped from **−127.40 to +85.29 per share** (ladder-off arm; +80.94 ladder-on). See [`analysis/s4b-measurement/RESULTS.md`](../analysis/s4b-measurement/RESULTS.md). The live path carries the same guard — the cron's per-tick fetch now stamps, and unrefreshed keys are skipped there too. |
| Effort | **S** for the eviction; **M** including the before/after ledger diff and the live-path check. |
| Priority | _Signed off 2026-08-31._ |

---

### S9. A carryover `OPEN` row silently consumes a parallel-trades slot forever

| | |
|---|---|
| Where | [`OrderService.forceCloseOpenPositions`](../src/main/java/com/moneymaker/order/service/OrderService.java) selects `findByStatusAndEntryTimeBetween(OPEN, startOfToday, endOfToday)`; the same `OPEN` rows are what the parallel-trades cap counts when admitting a new signal. Surfaced by the GAPS #1 live force-close work (see the note under that entry) — filed here because of the entry-capacity half. |
| Why | The end-of-day sweep only ever sees positions *entered today*. A row left `OPEN` by a previous day — JVM down at 15:31, or a live exit that failed and was never squared off — is invisible to every later sweep: never force-closed, never alerted. The infrastructure half (a stranded broker position) lives in GAPS #1's note; the reason this entry exists is the strategy half: that stale row **keeps counting against `numberOfParallelTrades` for its config indefinitely**, so the config can be blocked from entering new trades by a position that no longer meaningfully exists. |
| Impact | **Unquantified.** Requires the failure to occur first (a missed 15:31 sweep or failed exit), so a ledger scan is the measurement: count `OPEN` rows with `entry_time` before the current session across the historical DB, and check whether any config's admission was refused while one existed. |
| Fix sketch | Widening the sweep's selector to all `OPEN` rows is a one-line change, but it decides whether a stale row gets a market exit at *today's* price — a real-money call needing sign-off. The cap-counting side could alternatively exclude rows older than the session from the parallel-trades count, which changes what gets entered and is equally Rule 0. Neither attempted. |
| Decision (Rule 0) | **2026-08-31 — signed off by the user, per the recommendation: widen the sweep.** The end-of-day force-close now selects *all* `OPEN` rows, so a carryover row gets a market exit at the sweep's price (alerted in live via the existing force-close failure alerts if the exit cannot be placed) and thereby frees its parallel-trades slot. In a replay every row is closed by its own day's sweep, so the backtest ledger is expected unchanged — verified against the S5 Arm After baseline. |
| Effort | **S** for either lever; the decision is the work. |
| Priority | _Signed off 2026-08-31._ |

---

### S10. A structure-based exit is now measurable and still unmeasured — nothing reads the during-position timeline

| | |
|---|---|
| Where | [`PositionJournal`](../src/main/java/com/moneymaker/journal/PositionJournal.java) writing `MONITOR` / `EVENT` rows from [`PositionService.handleOne`](../src/main/java/com/moneymaker/position/service/PositionService.java); detector [`MarketStructureAnalyzer`](../src/main/java/com/moneymaker/structure/MarketStructureAnalyzer.java). Exits themselves: `PositionService.thresholdBreach`, `trade_config.stop_loss` / `max_sl_points` / `trail_ladder`. |
| Why | Filed here rather than in `GAPS.md` because the thing it leads to is an **exit rule**, which decides what trades get taken. As of 2026-08-31 the journal records, per open trade per tick, whether a BOS or CHoCH had become knowable — `WITH` or `AGAINST` the position — with the minutes of warning it gave and the trade's P&L at that moment. That was built precisely to test the premise stated in [`OBSERVATION_JOURNAL.md`](OBSERVATION_JOURNAL.md): against a ledger where **all 21 stop-losses overshot their stop** and 17 trades gave back an average of +13.81 before closing at −77.19, an `AGAINST` CHoCH may be an earlier and cheaper exit signal than a percentage or points stop. **Nothing acts on these rows, and nothing reads them.** No strategy, no rule, no query. |
| Impact | **Unquantified — and deliberately so; no behaviour has been changed.** What would measure it, now that the rows exist: replay a window with `journal.enabled=true`, then for each closed trade join its `EVENT` rows to its `EXIT` row and ask (a) how many stopped-out trades had an `AGAINST` CHoCH confirmed before the stop fired, (b) the distribution of `monitor_minutes_since_entry` between that event and the actual exit — the warning in bars — and (c) the counterfactual P&L of exiting at the event's bar close instead. `monitor_pnl` and `monitor_peak_profit` are on the event row so (c) needs no second replay. The control is the same window with the current bracket, which [S4](#s4-the-036-exit-bracket--sl-ceiling--trailing-ladder--is-shipped-but-unmeasured) already ran (2024-01, Run B: −141.10 per share, 6 `TRAIL_SL` giving back 219.50). **First volume data (2026-08-31, the S5 arms):** a one-month replay wrote 9,289 `CANDIDATE` / 21 `ENTRY` / 261 `MONITOR` / 77 `EVENT` / 21 `EXIT` rows — MONITOR is bounded by open-trade ticks as designed, and the rows are now sitting in `journal_observation` ready for exactly the join this entry describes. |
| Two things the measurement must respect | **(1) `confirmableAt`, not `occurredAt`.** The journal only writes an event once its confirming bar has settled, and any query must filter on `confirmable_at` for the same reason — reading at `occurredAt` credits the strategy with a level before the market finished drawing it, the look-ahead that cost this codebase a 640-point swing in apparent edge. **(2) The counterfactual is not free.** Exiting on a CHoCH would also exit trades that went on to reach target, so (c) must be computed over *all* trades with a qualifying event, not only the ones that stopped out. |
| Measured (2026-08-31, full-2024 replay `bt-2024-01-01_2024-12-31-1788159770820`) | The join this entry asked for is now run over a full year: 161 trades, 374 EVENT rows -- all in-position by construction, and `entry_time <= confirmable_at <= exit_time` holds on 374/374, so the recorder respects requirement (1). **(a)** 31/161 trades (19.3%) had an AGAINST CHoCH confirmed in-position; 17 of the 52 STOP_LOSS trades (32.7%) had one confirmed before the stop fired. **(b)** Warning on those 17: avg 10.59 min, min 0 (same tick), max 55 min. **(c)** Counterfactual "exit at the first AGAINST-CHoCH tick's `monitor_pnl`", computed over all 31 qualifying trades per requirement (2): actual sum **-504.46** vs counterfactual **-715.70** per share -- **the naive blanket rule costs ~211 points on 2024**. Decomposition: it saves ~+103 on the 17 stop-outs (avg -33.90 vs -39.97) but kills 5 trades that recovered to TARGET (avg +34.57 actual vs -12.04 counterfactual) and 3 that recovered to TRAIL_SL (+2.00 vs -18.95). Separately, the *predictive* value is strong even though the naive rule is not: trades with any AGAINST event (CHoCH or BOS) mid-trade ran 28.81% win rate / avg -17.41 (n=59) vs 83.33% / +16.90 with none (n=102). **Verdict: the blanket CHoCH-against exit is measured and rejected by its own numbers. The open question is now a *filtered* variant (e.g. only while `monitor_pnl` < 0, or after a minimum trade age) -- and 31 qualifying trades in a year is too small a sample to fit such a filter without overfitting; it needs more windows or forward data.** Caveat: the 3 FORCE_CLOSE trades have no journal EXIT row ([GAPS #24](GAPS.md#24-eod-force-close-writes-no-journal-exit-row)), a negligible hole here but real for any future journal-only exit analysis. |
| Fix sketch | Nothing until measured **and signed off** — a structure-driven exit changes when every open trade closes, so it is Rule 0 twice over. If it is ever wired, note that `MarketStructureAnalyzer.DEFAULT_FRACTAL_N = 2` is documented as a *measurement* constant that stays in code only while structure decides nothing; the moment it gates an exit it becomes a trading parameter and must move to `TradeConfig` (CLAUDE.md invariant 9), along with any "which series" and "WITH/AGAINST" selection. |
| Effort | **M** for the measurement (a replay plus queries — the capture is already built). **L** for any exit rule that follows: new config fields, a before/after pair, and sign-off. |
| Priority | **Measurement done 2026-08-31** (row above) — the naive rule is rejected; whether to pursue a filtered variant is the user's call. No behaviour changed. |

---

### S11. Wall-clock scheduler threads mutate replay state mid-run

| | |
|---|---|
| Where | [`TradeConfigScheduler.checkTradeConfigAt916AM`](../src/main/java/com/moneymaker/scheduler/TradeConfigScheduler.java) — `@Scheduled(cron = "0 16 9 * * MON-FRI")`, ungated by mode. Historically also the three pipeline crons, gated 2026-08-31 under [`GAPS.md` #4](GAPS.md#4-pipeline-cron-annotations-fire-in-backtest-mode-too--resolved-2026-08-31). |
| Why | A backtest replay and the live cron threads share one JVM and one set of statics. `SharedData.combinedDto` — the list of `(config, strategy)` pairs the strategies are dispatched over — is assigned by `BacktestAnalysisService.runForDate` at the top of **every** tick, and independently by this cron at wall-clock 09:16 from *today's* DB rows. The per-tick reassignment means the exposure is a race, not a standing clobber: the cron thread can swap the list between the replay's `calculateIndicator(asOf)` and its `runStrategies(asOf)`, so that one tick dispatches today's live configs against the replayed window's cached candles. It also seeds the date-keyed config cache with today's date and, if `telegram.backtest-enabled=true`, fires the day's config report out of a replay. |
| Why it is here and not in `GAPS.md` | It decides **which configs get dispatched**, i.e. what trades get taken — the `GAPS.md` #4 half of the same problem is filed there because it was cron plumbing; this half is not. |
| Impact | **Unquantified.** Two separate things need measuring, and neither is done. **(a)** The live race above: replay a fixed window with the JVM clock crossing 09:16 on a weekday and diff the ledger against the same window replayed outside that minute — a single-tick divergence is what a hit looks like, so the diff must be per-order, not on totals. **(b)** More consequential: **every backtest ledger recorded before 2026-08-31 came from a JVM where the three pipeline crons ran their full bodies in backtest mode** (see the `GAPS.md` #4 note — the market-hours guard only short-circuits in live mode, so it never fired in backtest). Any run started on a weekday between 09:00 and 16:55 IST had a second wall-clock pipeline draining `SharedData.tradeSignals`, writing today's candles into the strike map, and monitoring the replay's OPEN rows against wall-clock quotes. That covers the numbers recorded in [S4](#s4-the-036-exit-bracket--sl-ceiling--trailing-ladder--is-shipped-but-unmeasured), [S6](#s6-target_pct--sl_pct-never-reach-the-running-pipeline--the-percentage-bracket-has-never-actually-run), the S7 re-entry count and the S8 observation. Measuring it means re-running those windows on the post-fix code and comparing — cheap, since the windows and the arms are already written down in each entry. **(b) measured 2026-08-31:** the [S5](#s5-session-window-constants-are-hardcoded-while-appmarket-already-exists--resolved-2026-08-31) baseline arm re-ran the Jan-2024 window on the post-fix build (journal MONITOR/EVENT active) and reproduced the S6 Arm B ledger **byte-identically modulo autoincrement id** — the recorded S4/S6/S7/S8 numbers for that window are confirmed uncontaminated. Part (a), the live 09:16 race, remains unmeasured. |
| Fix sketch | For the remaining `TradeConfigScheduler` cron, the same shape `GAPS.md` #4 used: gate the `@Scheduled` entry point on `app.mode`, leaving `getConfigsForDate` / `reportConfigsForDay` untouched so the replay's own calls are unaffected. That is infrastructure and could be done without sign-off — **but it changes what a replay dispatches**, so per working rule 3 it needs the user first, and ideally lands with the re-measurement in (b) rather than silently shifting the baseline underneath the recorded numbers. |
| Decision (Rule 0) | **2026-08-31 — signed off by the user: gate it.** Same wrapper shape as `GAPS.md` #4; replay call sites untouched; verified against the S5 Arm After baseline (expected unchanged — the gate removes a race, not a code path the replay uses). Part (a)'s live-race measurement remains open. |
| Effort | **S** for the gate; **M** including the re-measurement of the four entries above. |
| Priority | _TBD — found 2026-08-31 while gating the pipeline crons for `GAPS.md` #4, not yet raised with the user._ |

---

### S13. A config that leaves `SharedData.combinedDto` mid-day strands its open trades' broker exits

| | |
|---|---|
| Where | [`OrderService.findConfig`](../src/main/java/com/moneymaker/order/service/OrderService.java) reads `SharedData.combinedDto`; its callers are `closeOrder`, the manual close, and the force-close exit. `SharedData.combinedDto` is rebuilt from [`TradeConfigRepository.fetchCombinedByTradingDate`](../src/main/java/com/moneymaker/repository/TradeConfigRepository.java) by `TradeConfigScheduler` and by `TradeConfigAdminService.afterMutation`. |
| Why | An **exit** is sized from the cached config, not from the order row. `trade_order.quantity` has been snapshotted since changeset 029, but the placement services read `config.getTradeConfig().getLotQuantity()` and fall back to quantity `1` when the config is absent -- one unit against a 75-unit lot opens a position rather than closing one -- so `OrderService` treats a null config as "do not place". The ledger row is then marked `CLOSED` while the broker position stays open. That is one of the four paths `alertForceCloseExitFailed` was built for (GAPS #1), and the alert is the only thing standing between it and an overnight position. |
| How a config leaves the list mid-day | Three ways today, all reachable from the admin UI: editing `tradingDate` to another day, the bulk `force` delete, and (as of 2026-08-31) retiring a config via `is_active`. The first has existed all along and is precisely the workaround GAPS #7 was written to replace, so this is **not a hazard the retire feature introduced** -- it is one the retire feature made visible. |
| Filed here, not in `GAPS.md` | It decides whether an open trade actually gets closed at the broker, which is what trades get taken in the only sense that costs money. |
| What is already done | `TradeConfigAdminService.setActive` **refuses** to retire a config with OPEN trades, and the GAPS #8 confirmation gate now names a `tradingDate` change as consequential while trades are open. Both are guards at the admin surface; neither fixes the underlying resolution. |
| Impact | **Unquantified.** What would measure it: scan the ledger for rows whose `exit_reason` is set but whose `exit_broker_order_id` is null in live mode, and cross-check against `alert_state` / the Telegram log for a matching `alertForceCloseExitFailed`. A zero count would say the path has never been hit in production; a non-zero count names real stranded positions. Cannot be measured in backtest -- `BacktestingOrderPlacementService` never needs the config for an exit. |
| Fix sketch | Make exit-side config resolution not depend on the live cache. Two shapes, both Rule 0 because they change whether an exit is placed: **(a)** fall back to a `tradeConfigRepository.findById` when `findConfig` misses -- correct and cheap, but it means an exit can go out for a config the dispatch list has deliberately dropped, which is the *intent* here but should be stated rather than inherited; **(b)** size the exit from `trade_order.quantity`, the value the entry actually carried, and stop consulting the config for exits entirely -- arguably the right answer, since an exit should mirror its own entry rather than today's configuration, but it changes the quantity on every live exit path and needs a before/after on a live-mode ledger, not a replay. Neither attempted. |
| Effort | **S** for (a), **M** for (b) including the verification. The decision is the work. |
| Priority | _TBD -- filed 2026-08-31 while implementing GAPS #7; not yet raised with the user. No behaviour was changed on the basis of this entry beyond refusing the one new way to trigger it._ |

---


### S14. CE legs outperform PE legs ~9x on the 2024 replay -- unexplained asymmetry

| | |
|---|---|
| Where | Full-2024 Strategy1 replay ledger (run `bt-2024-01-01_2024-12-31-1788159770820`, 161 trades, `HISTORICAL_ICICI`, post-S8 / resting-order build). Sides come from `trade_config.trading_side` (the AUTO_DOWNTREND detector generates both; 82 CE / 79 PE trades, and `trading_side` matches `option_type` on every row). |
| What was measured (2026-08-31) | CE: 82 trades, **+623.35** per share, 69.51% win rate, avg +7.60. PE: 79 trades, **+73.19**, 56.96%, avg +0.93. ~90% of the year's +696.54 came from the CE side. All 3 FORCE_CLOSE exits were PE; STOP_LOSS split CE 23 / PE 29. No entry-time journal feature examined (structure state, OI regime, premium level, day-range position) separates the two sides -- the asymmetry is unexplained, not merely unquantified. |
| Impact | The observed split is measured (above). The impact of any *change* (tightening or dropping the PE side) is **Unquantified** -- and a plain "subtract the PE trades" read is invalid: the S4/S6 measurements both showed that removing trades frees `no_of_parrellel_trades` slots and admits different entries. The measurement is a paired replay with PE configs withheld. |
| Fix sketch | Nothing until understood. Diagnosis first: split CE-vs-PE by month/regime (a losing PE stretch reads very differently from a structural edge), and check whether the detector's config generation is symmetric in practice. Any side-specific rule is Rule 0: sign-off plus a paired run. |
| Effort | **M** for the diagnosis; the paired run is the standard harness. |
| Priority | _TBD -- filed 2026-08-31 from the full-2024 insights pass; not yet raised with the user._ |

---

### S15. `5min/SMA100` is the only losing entry-signal type on the 2024 replay

| | |
|---|---|
| Where | Same full-2024 ledger as [S14](#s14-ce-legs-outperform-pe-legs-9x-on-the-2024-replay----unexplained-asymmetry). `entry_reason` split: `5min/SMA50` 57 trades +245.39 (66.7% WR), `15min/SMA50` 48 trades +295.07 (64.6%), `5min/SMA200` 24 trades +320.74 (66.7%), **`5min/SMA100` 32 trades -164.66 (53.1%)**. |
| Why | One of the four active (timeframe, primary-SMA) combinations loses money over a full year while the other three are solidly positive on comparable samples. Whether SMA100-on-5min is structurally worse or just unlucky on this window is exactly this file's kind of question. |
| Impact | The observed split is measured. The impact of disabling or re-tuning the combination is **Unquantified** -- same slot-freeing caveat as S14: the measurement is a paired replay with the combination withheld, not ledger subtraction. |
| Fix sketch | Nothing without sign-off. Note the lever is config data (`sma_timeframe` rows), not code -- withholding the combination for a paired run needs no code change. |
| Effort | **S** to withhold in config for a paired run; **M** with the measurement. |
| Priority | _TBD -- filed 2026-08-31 from the full-2024 insights pass._ |

---

### S16. ~79% of the 2024 profit is Thursday trades -- expiry-day concentration

| | |
|---|---|
| Where | Same full-2024 ledger as [S14](#s14-ce-legs-outperform-pe-legs-9x-on-the-2024-replay----unexplained-asymmetry). Day-of-week: Mon 1 / Tue 19 / Wed 57 (+159.78) / Thu 82 (**+549.25**, 65.9% WR) / Fri 2. |
| Why | The flow clusters into Wed/Thu (weekly-expiry premium decay is richest there), so the annual result leans heavily on one weekday: a single adverse expiry-day event (gap, holiday-shifted expiry) swings an outsized share of the year. Not a defect -- an exposure fact to hold against any annual number from this strategy. The Thursday edge is per-trade as well as volume: avg +6.70/trade Thu vs +2.80 Wed. |
| Impact | The concentration is measured (above). Whether behaviour *should* change is open; any day-of-week rule is a new trading parameter and belongs in `TradeConfig` (CLAUDE.md invariant 9), gated on its own paired measurement. |
| Fix sketch | None proposed; diagnosis first -- is the non-Thu residue (79 trades, +147.29) worth its risk, and is the Thursday expectancy stable across the year's months? The journal already holds everything needed. |
| Effort | **S** for the diagnosis queries. |
| Priority | _TBD -- filed 2026-08-31 from the full-2024 insights pass._ |

---

### S17. The `indicator_type` seam ships with one scanner — which indicators earn a second is unmeasured

| | |
|---|---|
| Where | [`EodTrendScanner`](../src/main/java/com/moneymaker/tradeconfig/generation/EodTrendScanner.java) (changeset 039) — the auto-config detector now dispatches its scan by `sma_downtrend_rule.indicator_type`, with [`SmaDowntrendScanner`](../src/main/java/com/moneymaker/tradeconfig/generation/SmaDowntrendScanner.java) as the only implementation. Same changeset made the SMA grid per-rule (`sma_periods` / `timeframes_minutes`). |
| Why | The seam was built on the user's request ("add different indicator rule — I need provision"), but *which* indicator (RSI? EMA? something else), on which series, with what thresholds, is an open trading decision — every threshold belongs in new rule columns (CLAUDE.md #9), and none has been chosen. Two sub-questions ride along: (a) a non-SMA scanner must still emit `(sma, timeframe)` combos, because those become the generated config's `sma_timeframe` children and the strategies' primary periods — an indicator with no natural SMA mapping needs a convention for that; (b) SMA-20 is now selectable for *detection* while the strategies' SMA-20 rule case stays commented out, so a 20-period combo generates configs that never trade until that case is deliberately re-enabled — the fail-closed default, kept on purpose. |
| Impact | **Unquantified.** No behaviour changed — the 039 defaults reproduce the old hardcoded grid bit-for-bit. The measurement, when an indicator is proposed: generate configs for the same window with the old and new `indicator_type` (or a narrowed `sma_periods`), replay both, compare ledgers. |
| Fix sketch | Nothing without the user naming the indicator and its thresholds. The mechanical path is documented in [EOD_DOWNTREND.md](EOD_DOWNTREND.md#skipping-smas--adding-a-different-indicator-rule). |
| Effort | **M** per indicator (scanner bean + threshold changeset + paired measurement). |
| Priority | _Blocked on a user decision — filed 2026-08-31 when the seam landed._ |

---

### S18. A coarse decision bar re-arms entries on every fine tick until the next bar settles — stop-out → immediate re-entry churn

| | |
|---|---|
| Where | Observed on config 2035's 2025-01-08 cross-run ledger (trades 2125–2128, all `15min/SMA50` CE SELL entries): four sequential entries in 75 minutes, three stopped out. The mechanics: a settled 15-min bar stays the newest decision bar for three 5-min ticks, and the strategy re-emits its signal on each; `OrderService`'s guards then decide what opens. The exact-duplicate guard blocks the *same strike* re-entering on the same bar (same `entryTime`), and `max_parallel_per_side` serialises the side — but the moment a stop-out frees the slot, the *other* in-band strike enters on the very same decision bar (2126 stopped out on candle 14:25; 2127 opened the next tick, still off bar 14:15). Nothing expresses "this bar already produced a stop-out, wait for fresh information". |
| Why | Whether re-entering on evidence that has already failed once is good or bad is exactly this file's kind of question. On this sample it was bad — three consecutive `STOP_LOSS` exits on one config in one afternoon — but one afternoon is not a measurement. Note the trade-count lever already exists and was simply unset here: the config ran with `no_of_trades = NULL` (uncapped) because the strategy-2 `strategy_defaults` row leaves it null. |
| Impact | **Unquantified.** Measure on a full replay: count entries whose decision bar equals a same-day earlier stop-out's decision bar (same config), and compare that subset's P&L against the rest. Cheap query — `entry_time` *is* the decision bar. |
| Fix sketch | Nothing without sign-off. Candidate levers, all config-shaped per CLAUDE.md #9: set `no_of_trades` in `strategy_defaults` (works today, no code); or a per-bar entry budget / post-stop-out cooldown as a new `TradeConfig` column. The display side is worth noting either way: ledger entry/exit stamps are candle times, so serialised trades can *look* concurrent — see the 2126/2127 walkthrough in the session notes. |
| Effort | **S** for the measurement; **M** for a cooldown column with its paired replay. |
| Priority | _TBD — filed 2026-08-31 from the config-2035 cross-run ledger review._ |

---

### S19. `Strategy3`'s mirror equivalence to Strategy 1 is structural, not tick-exact — and unmeasured

| | |
|---|---|
| Where | [`Strategy3`](../src/main/java/com/moneymaker/strategy/Strategy3.java) + [`RuleEngine.decideBuyEntry`](../src/main/java/com/moneymaker/strategy/rules/RuleEngine.java), added 2026-09-04 on the user's request: *"strategy 3 … does buy instead of sell … rule sets are same as that of strategy 1 just inverse the signal … if there is a PE sell signal we will make it CE buy"*. Documented in [STRATEGIES.md](STRATEGIES.md#strategy-3--the-inverted-baseline-buy-side). |
| Why | The request pairs a signal on one leg (PE cross-down SELL) with a trade on the *other* leg (CE BUY). The architecture scans and trades only the config's own `trading_side`, so the build realises the pairing by mirroring the detection onto the traded leg's own series: CE cross-**up** + `isSmaNNUpTrending` → BUY. That fires on the same market move as the PE sell, but not necessarily on the same bar — the two legs have different premiums, different SMA levels, and independently-stamped trend flags (with `maxDeviations = 0`, one flat CE bar kills `UpTrending` for the CE's whole session while the PE's `DownTrending` survives). The alternative — exact-tick coupling, where the CE config literally consumes the PE config's signal — was **not** built: it would cross config cache boundaries, contradict the `(trading_side ↔ option type)` ledger invariant restored by [S2](#s2-strategy1-scanned-every-configs-legs-not-its-own--resolved-2026-08-25), and need a strike-mapping convention (same strike? mirrored moneyness?) that has no `TradeConfig` field yet. |
| Impact | **Unquantified.** Two measurements wanted: (a) replay a window with a PE config on strategy 1 and its CE sibling on strategy 3, and join the two signal streams by session — how often does the CE buy have a matching PE sell within 0 / 1 / 3 bars, and how often does either fire alone; (b) the strategy's own ledger P&L over the same window, since long-option economics (theta against the position, SL/target geometry inverted) differ from the short side even when timing matches perfectly. |
| Fix sketch | Nothing until measured. If (a) shows the mirror diverging materially, the exact-tick design above is the fallback to propose — as its own entry, with the strike-mapping question put to the user per CLAUDE.md #9. |
| Effort | **S** for measurement (a) — signal-stream join over one replayed month; **M** for (b) with a written verdict. |
| Priority | _Open — filed 2026-09-04 at build time; the strategy ships with this question attached._ |

---

### S20. `Strategy4` buys the falling option — the fade is specified but unmeasured

| | |
|---|---|
| Where | [`Strategy4`](../src/main/java/com/moneymaker/strategy/Strategy4.java) + the `mapAction` seam in [`AbstractSmaCrossStrategy`](../src/main/java/com/moneymaker/strategy/AbstractSmaCrossStrategy.java), added 2026-09-04 on the user's request: *"execute strategy 1, whenever we get sell signal instead of sell we will do buy … do not change the rule set of sell … rest remains same — profit, sl, stop loss move etc"*. Built exactly as specified: detection is byte-identical to Strategy 1 (a `1,4`-tagged config detects on the same ticks under both), only the emitted action inverts. Documented in [STRATEGIES.md](STRATEGIES.md#strategy-4--strategy-1s-detection-inverted-at-execution). |
| Why | The trade this opens is a long in an option whose premium just crossed **down** through its SMA with the whole-day down-trend flag set — long against the detected momentum, with theta also against the position (a long needs the premium to *rise* to profit, and the same absolute `target` is now above entry instead of below). It may be a deliberate mean-reversion fade and it may work; nothing about it is measured. Note the bracket numbers were tuned on the short side: the S4b measurement showed the ladder/ceiling geometry interacting with *falling*-premium trades, and none of that transfers by symmetry. Also worth knowing when reading a `1,4` ledger: the two strategies open opposite directions off one bar, so their rows pair up with identical `entry_time` and mirrored `entry_direction` — expected, not a duplication bug. |
| Impact | **Unquantified.** The cheap first measurement: tag one window's configs `1,4` (with a `transaction_type = BUY` clone for the 4 side — one config row cannot carry both directions, so it is a cloned config pair, same legs and numbers), replay, and compare the two strategies' ledgers row by row — same entries by construction, opposite P&L before brackets, diverging after brackets since SL/target/trailing trigger at different times per side. A three-way window against strategy 3 (mirror-detection long) on the same dates would also answer which inversion the user actually wants to keep. |
| Fix sketch | Nothing to fix — the build matches the specification. This entry exists so the fade ships measured rather than assumed; per working rule 3, any behaviour change that measurement suggests goes back to the user first. |
| Effort | **S** for the paired replay; the config cloning is the only fiddly part. |
| Priority | _Open — filed 2026-09-04 at build time, alongside [S19](#s19-strategy3s-mirror-equivalence-to-strategy-1-is-structural-not-tick-exact--and-unmeasured)._ |

### S21. Entry detection reads one strike's premium, which steps as spot walks the strike grid

| | |
|---|---|
| Where | [`SMAIndicatorImpl`](../src/main/java/com/moneymaker/indicator/SMAIndicatorImpl.java) (SMA over candle **lows**, per option token) feeding the cross predicates in [`strategy/rules/`](../src/main/java/com/moneymaker/strategy/rules/) via the per-leg series `AnalysisScheduler` writes into `SharedData.strikeMarketDataByInstrumentAndInterval`. Filed 2026-09-04 while adding the dashboard's averaged ATM±1 / ATM±2 panes ([CHART_DASHBOARD.md](CHART_DASHBOARD.md#averaged-strike-ladders-atmn-panes)) — the panes exist because the user wanted the ladder average visible next to the single strike. |
| Why | Every entry decision is a cross of one contract's premium against its own SMA. A single strike's premium carries a component that is not about direction at all: as spot walks across the strike grid the same contract slides between ITM, ATM and OTM, and its delta — and so its sensitivity to the next point of spot — changes underneath a series the indicator treats as homogeneous. The mean of a ladder straddling the money does not have that component, which is the whole reason the new panes were asked for. **Nothing here says the strategy is wrong.** The single-strike series is also the thing actually traded, and an average of five contracts is not an instrument — a signal off it would still have to be executed on one leg. The open question is narrower: *does the averaged series cross at materially different bars than the traded leg does?* If it does not, the noise is cosmetic and this entry closes. |
| Impact | **Unquantified — and deliberately not guessed.** The measurement does not need a code change to start: for one replayed window, pull the traded leg's 5-minute series and the ATM±1 / ATM±2 averages for the same expiry from `historical_option_candles`, compute `SMA(low)` on each at the config's period, and count bars where the cross direction disagrees. That is a read-only script over data already imported — the chart panes render exactly these three series, so a first look is a page load, not a run. Only if the disagreement rate is non-trivial does the expensive version follow: a paired backtest, one arm gating on the averaged series, on the same window, per working rule 3. |
| Fix sketch | Nothing to fix and **nothing to change without the user** — this is a live entry rule. If the disagreement turns out to matter, the shape would be a config-driven choice of gating series (single strike, as today, vs an averaged ladder of configurable width), never a hardcoded switch: per Rule 9 the width would be a `TradeConfig` column, and today there is no such field, so it has to be asked for rather than assumed. Note the averaged series would also need the all-legs rule the chart uses — averaging whichever legs happen to be present steps the level and manufactures crossings, which would be the exact opposite of the point. |
| Effort | **S** for the disagreement count (read-only, existing data). **M+** for anything beyond it — a new config column, a second gating path, and a before/after pair. |
| Priority | _Open — filed 2026-09-04 at build time of the averaged chart panes. Not blocking anything; the cheap measurement is worth doing before the expensive question is even asked._ |

### S22. Pressure's "VWAP" term is a session typical-price mean — and was briefly implemented as something else

| | |
|---|---|
| Where | [`SessionAnchoredPrice`](../src/main/java/com/moneymaker/indicator/series/SessionAnchoredPrice.java), selected by `app.pressure.anchor-mode` in [`SpotFeatureCache`](../src/main/java/com/moneymaker/strategy/pressure/SpotFeatureCache.java). Consumed as one of the four confirming terms in [`PressureScore`](../src/main/java/com/moneymaker/strategy/pressure/PressureScore.java). Filed 2026-09-05; **rewritten the same day** after the strategy's author corrected the premise. |
| What happened | The written spec says *"Session VWAP from 09:15, typical price (H+L+C)/3, expanding within the day"*. That was read literally. NIFTY is an index and has no traded volume — 19,572 of the 19,602 `historical_spot_candles` rows for 2024 carry `volume = 0` — so rather than ask, the first implementation **invented a substitute**: it weighted each bar by CE+PE volume summed across the front weekly expiry. The author then clarified (2026-09-05): *"The word VWAP in Pressure = not real VWAP. I should have called it session typical-price mean… No volume in the formula."* The reference book of 1,560 tickets was produced with an unweighted expanding mean of HLC typical price, exactly what this codebase had written only as a degenerate fallback. |
| Why it mattered | This is not a naming quibble. `close vs anchor` is one of four confirming terms against a 3-of-4 threshold, so a bar where the weighted and unweighted series sit on opposite sides of `close` **flips the signal outright**. Every figure from the affected build was therefore measuring a different strategy, and nothing in the logs, the summary table or the trade CSV would have looked wrong. The general lesson is the one working rule 2 already states in a different form: an input that cannot be computed from the available data is a **question for the author**, not an invitation to model a substitute. |
| Resolution | `TYPICAL_MEAN` is now the default and reproduces the reference formula. Pinned by [`SessionAnchoredPriceTest`](../src/test/java/com/moneymaker/indicator/series/SessionAnchoredPriceTest.java), whose first assertion exists specifically to fail if a weight is reintroduced under the banner of "fixing the VWAP". The class was renamed from `SessionVwap` so the code stops asserting something untrue. |
| Still open — the real-volume experiment | The author proposed the honest version of the original idea: a genuine volume-weighted price built from the **option tape** (ATM ±2 strikes, both rights) rather than from a spot volume column that does not exist. That is available today as `app.pressure.anchor-mode=OPTION_TAPE_VWAP`, but the current weighting is the whole front-weekly chain, not an ATM ±2 band, so the mode as shipped is **not** the proposed experiment. |
| Impact | **Unquantified, deliberately.** Two separate unknowns and they must not be conflated: **(a)** how much the mis-implemented weighting moved the January slice — measurable now by re-running the same window under both modes and diffing the entry set; **(b)** whether an ATM ±2 option-tape VWAP is *better* than the typical-price mean, which is a strategy question, not a bug. (b) requires the reference book to be re-marked against it before any comparison means anything — the author has offered to do that if asked. |
| Fix sketch | For (b), narrow the weight query from the whole chain to ATM ±2 strikes both rights, which needs the ATM per bar and therefore the spot close — an input `volumeWeights` does not currently take. Keep it opt-in; do not make it the default without a paired before/after per working rule 3, and not without the re-marked reference. |
| Effort | **S** for (a). **M** for (b): a reshaped query plus a paired run plus a re-marked reference book. |
| Priority | _Resolved for correctness (the default now matches the reference). Open only as the (b) experiment, which is the author's call to commission._ |

### S23. Pressure's trail tests the bar's wick, while its spec says test the close

| | |
|---|---|
| Where | [`PositionService.thresholdBreach`](../src/main/java/com/moneymaker/position/service/PositionService.java) — floors are tested against `adversePnl` (the bar's adverse extreme) and fill at the floor price. Applies to Strategy 5's `25:25` give-back trail; see [PRESSURE_STRATEGY.md](PRESSURE_STRATEGY.md#the-2525-trail). Filed 2026-09-05. |
| Why | The Pressure spec says "Use bar high/low for stop/target (intrabar), **close for trail test**". This codebase does not distinguish: since the S4 resting-order decision (2026-08-31) every floor — fixed stop or trailing rung — is treated as an SL order resting at the broker, so it fills the moment a bar touches it. Keeping that model for Strategy 5 was a deliberate user decision (2026-09-04) rather than an oversight: it is consistent with every other strategy here, needs no change to shared exit code, and is the more conservative of the two. But it **is** a divergence, and it runs one way — a wick through +25 that closes at +40 exits here and would not exit under the spec, so this book will show more trail exits and shorter holds than the reference. |
| Impact | **Unquantified.** Measurable directly and cheaply once a Pressure run exists: count `TRAIL_SL` exits whose bar *close* was still above the floor — those are exactly the trades the two models disagree on. Their realised P&L versus what the position would have done had it survived to the next bar is the size of the divergence. Doing better than that requires a second exit path and a paired run. |
| Fix sketch | If the count turns out to matter, the shape would be a **config-driven trail test mode** (`ADVERSE_EXTREME`, as today, vs `CLOSE`) rather than a switch baked into `PositionService`, so strategies 1-4 keep the resting-order model untouched and only Strategy 5 opts out. Per Rule 9 that is a `TradeConfig` column, which does not exist today — so it has to be asked for, not assumed. **Do not change the shared floor test in place**; it is the exit model for every strategy in the system. |
| Effort | **S** to count. **M** for the config-driven mode plus a before/after pair. |
| Priority | _Open — filed 2026-09-05 at build time, as an accepted-and-recorded deviation rather than a defect. Revisit when the first full Pressure run is reconciled against the reference figures._ |

### S24. Pressure's entry fill is the signal candle's close, not the last close at or before the signal

| | |
|---|---|
| Where | [`Strategy5.execute`](../src/main/java/com/moneymaker/strategy/Strategy5.java) stamps `TradeSignal.price` from the resolved leg's last cached candle; `OrderService.openOrder` writes it to `trade_order.entry_price`. Filed 2026-09-05. |
| Why | The spec says "Entry premium = last option close at or before signal timestamp on that contract. If no bar, first bar after." The framework convention is the signal candle's own close, which is the same thing whenever the leg's series is current — and is **not** the same thing when the leg's series lags the spot bar that fired the signal. The S8 freshness stamp makes the lagging case *skip* the trade rather than fill it at a stale price, so the divergence surfaces as a missing trade rather than a mispriced one — the safer direction, but still a difference in trade count. Note also that the decision bar is spot while the fill is an option: `signalTime` is deliberately the spot bar's timestamp, so `entry_time` and the leg's own bar can differ by up to one bar in a degraded fetch. |
| Impact | **Unquantified.** Count Pressure ticks where a side fired but `resolveTradableKey` returned null — those are the trades the spec would have taken and this does not. The log line already exists (`fired but no tradable leg cached`, DEBUG). If the count is near zero the divergence is theoretical and this entry closes. |
| Fix sketch | Nothing to change unless the count is material, and not without the user. The candidate would be to fall back to the leg's last available close when the freshness stamp fails — which trades a missing trade for a stale-priced one, a strictly worse failure mode unless the staleness is bounded. That bound would have to be specified first. |
| Effort | **S** to count from existing logs. |
| Priority | _Open — filed 2026-09-05 at build time. Lowest of the three Pressure entries; likely theoretical, but unmeasured._ |

### S33. Strategy 8's chandelier is a tighter-of-two floor, and `2 x ATR` outruns the premium band on a third of entries

| | |
|---|---|
| Where | [`PositionService.thresholdBreach` / `applyTrail`](../src/main/java/com/moneymaker/position/service/PositionService.java), `strategy_defaults.trail_atr_multiple` (changeset 048). Filed 2026-09-06 from the [S31](#s31-first-engine-replay-of-strategy8-2024-depth-2-ladder-2715-pts--770-trades--but-the-leg-set-is-not-the-replays) ledger. |
| Why | [STRATEGIES.md](STRATEGIES.md) describes the exit as "a chandelier trail (peak - 2 x ATR-14)", which reads as *the* exit. In the engine it is one of **two** floors: `thresholdBreach` takes whichever of the fixed stop (`LEAST(entry x sl_pct, max_sl_points)`) and the chandelier (`peak_profit - trail_atr_distance_at_entry`) sits higher, and labels the exit `STOP_LOSS` or `TRAIL_SL` accordingly. Which one governs is decided by the leg's own volatility, not by design. |
| Impact | **Measured on the 770-trade ledger.** `trail_atr_distance_at_entry` (2 x ATR-14 of the 15-minute series) ranges **18.11 to 301.35 points** while the fixed stop is capped at 60 and the premium band is 80-250. On **259 of 770** entries (34%) the chandelier is already tighter at entry and governs from the first monitored bar - 175 of those exit `TRAIL_SL`, and the group averages **+4.86/trade (+1,260)**. On the other 511 the fixed stop governs first and the chandelier only overtakes it once peak profit has climbed; 140 still exit `TRAIL_SL`, and the group averages **+2.85/trade (+1,456)**. At the top of the range a 301-point trail distance on a leg whose premium is at most 250 and whose fixed stop is at most 60 cannot be reached before the fixed stop fires - the trail is structurally unreachable there, not merely loose. |
| Open question | **"Does the trail need fixing?" — answered 2026-09-06: no.** Counterfactual over the 315 `TRAIL_SL` exits, re-priced with *no trail* (the fixed stop if the leg's later high reached it, otherwise the 15:00-bar close the engine itself uses for `FORCE_CLOSE`): actual **−3,448.1** vs no-trail **−4,899.4** — the trail is worth **+1,451.2 pts on its own exits** (avg −10.95 vs −15.55). 178 of the 315 would have gone on to hit the fixed stop, 98 were winners the trail cut short, 39 exited better than the close would have. Caveat: this holds the rest of the session fixed and ignores that a trail exit frees the slot for a later entry — which in this engine works *for* the trail, not against it. The tighter-of-two floor is doing what it was built to do, and the 301-point cases are simply legs where the fixed stop is the stop, which is the documented intent. What remains is an **optimisation** question, not a repair: whether a multiple other than 2.00 does better. The measurement is the sweep (1.0 / 1.5 / 2.0 / 3.0) over the same window and config set, reading pts/trade and the `TRAIL_SL` / `STOP_LOSS` split — four ~5-minute runs, no code change, since the value is already a `strategy_defaults` column. Not urgent. |
| Caveat | Do not read the two groups as a like-for-like comparison of the floors: membership is decided by ATR, so the split also sorts by leg volatility, and the +4.86 vs +2.85 gap may be the volatility rather than the floor. Separating them needs the multiple sweep above, where membership changes but the legs do not. |
| Fix sketch | **None required.** If the optional sweep ever shows a better constant, that is a live trading change (CLAUDE.md Rule 0(c)): confirm with the user, change the seed, re-run the same window before and after, record both numbers here. There is no admin endpoint for `trail_atr_multiple` (only `bracket-mode`), so a sweep edits `strategy_defaults` directly; `OrderService` reads the row per order, no restart needed. |
| Effort | **S** - four backtest runs on the existing config set. |
| Priority | _Open as an optimisation only — filed 2026-09-06; the fix-required question was closed the same day._ |

---

### S32. Look-ahead audit of the Strategy 8 replay: clean — but the fill model is stale-by-one-tick and stops fill with zero slippage

| | |
|---|---|
| Where | [`MarketDataService.dropIncompleteBars`](../src/main/java/com/moneymaker/market/service/MarketDataService.java), [`PositionService.monitor`](../src/main/java/com/moneymaker/position/service/PositionService.java), [`BacktestingOrderPlacementService`](../src/main/java/com/moneymaker/backtesting/BacktestingOrderPlacementService.java). Filed 2026-09-06 after auditing the [S31](#s31-first-engine-replay-of-strategy8-2024-depth-2-ladder-2715-pts--770-trades--but-the-leg-set-is-not-the-replays) ledger for look-ahead. |
| Why | Asked directly: is Strategy 8 reading candles it could not have seen? Audited the whole path. **No look-ahead found**, on four guards: the fetch upper bound is the tick time (`AnalysisScheduler` line 286 passes `analysisDateTime`); `dropIncompleteBars` keeps a bar only when `ts + width <= asOf`, so a forming bar never reaches a rule; the provider aggregates *after* slicing, which its own javadoc says is what stops `interval - 5` minutes of leakage; and `PositionService` skips any quote not strictly after `entry_time`, so the entry bar cannot close its own trade. Intra-bar is adverse-first, and a rung armed by a bar's own excursion cannot fire on that bar. |
| Impact | **Measured on the 770-trade ledger.** 770/770 entry fills equal the close of the *completed* 15-minute bar (the T+10 five-minute close); 2 coincidentally equalled the next bar's close, 3 the bar-start close. Minimum hold is exactly 15 minutes (one bar), zero exits at or before entry, entries bounded 09:15-14:45 and every `FORCE_CLOSE` at 15:20. The one timing artefact runs **against** the strategy: the replay tick grid is 09:20 + 15k while 15-minute bars complete at 09:30 / 09:45 / ..., so each decision is taken at T+20 but filled at the bar close (price at T+15) — worth **-0.93 pts/trade, -713.4 pts over 2024** versus filling at the decision tick's price. The engine therefore understates this configuration by roughly that much. |
| Open question | **Unquantified: stops and trails fill exactly at the floor with no slippage.** 487 of 770 exits (63%) are `STOP_LOSS` or `TRAIL_SL` fills priced at the floor itself under the S4 resting-order model. That is the one convention in the run that flatters rather than penalises, and it is unmeasured. The measurement: re-price every floor exit at the adverse extreme of the bar that triggered it (or at a fixed points/bps haircut) and re-total — no engine change needed, it is a query over the existing ledger plus the candle rows. |
| Caveat | `entry_time` / `exit_time` are stamped with the **bar's** timestamp, not the decision tick, so a row reading 09:30 was actually decided at 09:50. This does not affect P&L but it does mean the ledger's own clock is 20 minutes ahead of when the trade could have been placed — relevant to anyone diffing this ledger against a replica keyed on time. |
| Fix sketch | Nothing to change without the user. If the slippage measurement proves material, the candidate is a config-driven slippage field applied at floor exits (CLAUDE.md #9 — it is a number that changes P&L, so it belongs in `TradeConfig`, not a constant). |
| Effort | **S** for the slippage re-pricing query. |
| Priority | _Open — filed 2026-09-06._ |

---

### S31. First engine replay of `Strategy8` (2024, depth-2 ladder): +2,715 pts / 770 trades — but the leg set is not the replay's

| | |
|---|---|
| Where | [`Strategy8`](../src/main/java/com/moneymaker/strategy/Strategy8.java). Filed 2026-09-06 from the first full backtest run of the bean. Partially answers [S29](#s29-strategy8-is-the-intraday-form-of-the-20sma-15min-candle-rule--the-replays-edge-was-in-carrying-to-expiry)'s measurement request. |
| Why | S29 and S30 both asked for an engine replay of Strategy 8 to diff against the Python replica. One now exists. Run: 2024-01-01 -> 2024-12-31, `strategyIds=8`, `HISTORICAL_ICICI`, 498 hand-made configs (249 sessions x CE/PE), `transaction_type = SELL`, `itm_depth = otm_depth = 2`, one 15-minute SMA-20 `sma_timeframe` row, band 80-250, `sl_pct 0.30`, `max_sl_points 60`, `strategy_defaults` 8 (`target_mode NONE`, `trail_atr_multiple 2.00`). 6,225 ticks, 0 failures, 297s. Ledger cleared before the run. |
| Impact | **Measured, this configuration only.** 770 trades, **+2,715.1 pts**, +3.53/trade, 42.9% win rate, profit factor 1.202, max drawdown **-818** on the trade-sequenced equity curve. By exit: `FORCE_CLOSE` 283 @ +47.52 (+13,447), `TRAIL_SL` 315 @ -10.95 (-3,448), `STOP_LOSS` 172 @ -42.35 (-7,284) — **every point of the edge is the 15:15 close-signal exit; both stop paths are net negative.** By side: CE 407 (+1,915), PE 363 (+800). Monthly range -471 (Jul) to +633 (Nov), two losing months (Feb -24, Jul -471). Not comparable to S29's replica line (1,780 trades / +2.0 / +3,532 over **two** years on a **single ATM leg**): this scans a 4-leg ladder per side, so trade count per session and leg mix both differ. |
| Caveat | Depth 2 was chosen so [S30](#s30-indicator--price-action--volume-study-on-strategy-8-three-gates-transferred-the-rest-did-not--and-the-volume-gate-depends-on-leg-coverage)'s volume gate stays measurable on the same config set — S30's own caveat says a depth-0 config does not run the gate the study measured. That makes this run a valid A/B base for Strategy 9 but **not** the faithful reproduction of the replica [STRATEGIES.md](STRATEGIES.md) describes ("a hand-made config with a 15-minute row and depth 0"). Two further deviations: `instrument.strike_points` is 100 while the option data is on a 50-point grid, so the ladder lands on 100-multiples and ATM rounds down to the nearest 100, not the nearest 50; and the replica priced 1 pt/round-trip costs, which this ledger does not deduct. |
| Open question | **Unquantified: does the depth-2 ladder change the rule's edge, or only its trade count?** The measurement is the same window re-run on depth-0 configs (one ATM leg per side) with a cleared ledger, comparing pts/trade and PF rather than totals. Until that exists, +3.53/trade cannot be read as either agreeing or disagreeing with the replica's +2.0. |
| Fix sketch | Nothing to change. Two follow-ups, neither started: (a) the depth-0 arm above; (b) ~~the Strategy 9 arm on this identical config set~~ — run the same day once the JVM was restarted; numbers recorded in [S30](#s30-indicator--price-action--volume-study-on-strategy-8-three-gates-transferred-the-rest-did-not--and-the-volume-gate-depends-on-leg-coverage). |
| Effort | **S** for each follow-up run — the config set already exists. |
| Priority | _Open — filed 2026-09-06._ |

---

### S31. Optimising Strategy 9: the expiry gate is where the points went; VIX gates do not help

| | |
|---|---|
| Where | [`Strategy10`](../src/main/java/com/moneymaker/strategy/Strategy10.java) (changeset 050). Filed 2026-09-06 with the strategy. |
| Why | Forty-odd single changes to the replica's Strategy 9 (chandelier k 1.5–3 × cap 30/50%, volume threshold 1.5/3/off, candle 0.15–0.5/off, DTE 1/3/off, entry windows, CE/PE only, one trade a day, premium ≥ 100/150, India-VIX level/prev-day change/opening gap/20-day distance) and their combinations, scored on 2024 and 2025 separately. **Measured:** the exit, the volume gate and the sides are at a local optimum (every variant lost points in at least one year). Removing the DTE gate adds +529 pts over two years but widens the drawdown (−246 → −372); adding candle ≥ 0.35 and a 13:15 last-entry bar on top gives 714 trades, +4,609 gross, avg +6.5, PF 1.6, DD −207, worst month −54 (Strategy 9: 602 / +3,504 / +5.8 / 1.5 / −246 / −151), +555 in 2024 and +549 in 2025. VIX: no gate improved both years; loss months (Feb, Jun, Jul 2024) had flat or falling VIX; daily P&L correlates *positively* with the same-day VIX change for the CE-heavy Strategy 8 book (+0.17). |
| Impact | **Unquantified in this engine** — same status as S29/S30; the engine's 2024 Strategy 9 ledger reproduced the replica's trade count within 6% and the exit mix within 3%, so the projection (+20–30%) is reasonable but in-sample. |
| Fix sketch | Measure with a `10`-tagged 2024 run against the replica's `DTE gate off + candle 0.35 + before 13:30` ledger (vix_and_optimisation.xlsx, Combos). If it holds, the remaining lever is positional (S29). |
| Effort | **S** for the measurement. |
| Priority | _Open — filed 2026-09-06._ |

---

### S30. Indicator / price-action / volume study on Strategy 8: three gates transferred, the rest did not — and the volume gate depends on leg coverage

| | |
|---|---|
| Where | [`Strategy9`](../src/main/java/com/moneymaker/strategy/Strategy9.java) (changeset 049). Filed 2026-09-06 with the strategy. |
| Why | 74 features on every 15-minute bar (RSI 7/14, ADX/DI 15-min and daily, Supertrend, EMA 9/21, SMA slopes, 60-min slope, Choppiness, efficiency ratio, Bollinger width/%B, day range/ATR, prev-day range/body, consecutive closes, candle close position, opening range, position in the day range, gap, TWAP, near-ATM option volume vs trailing median, put/call volume and OI ratios, OI build by side, ATM straddle %, straddle-implied vol weekly and ~30-day, hour, DTE, prev-day ADX/DI regime) were conditioned on Strategy 8's replay trades; ~420 allow-rules per base were scored, the best per feature re-run in the Python engine, plus all pairs of the top-10 and triples of the top-6 (~200 engine runs) and a walk-forward (rule chosen on 2024 tested on 2025 and the reverse). **Measured (hold-to-expiry base +6.0/trade, +6,366):** near-ATM volume spike > 2× → +0.5/trade on a third of entries, gate +458 pts, DD −423 → −336, both years; candle closing on its low → +2.9 vs +9.7–19, gate +236 pts; DTE 0–1 → +0.5 vs +10–14, gate −276 pts but PF 1.43 → 1.60. Regime (fade the prev-day trend +12.3, follow it +2.9) did not survive the engine — skipping with-trend days lost 826 pts because the freed slots refill. RSI, Supertrend, EMA, ADX, 60-min slope, Bollinger width, Choppiness, OI build: no net gain. India VIX was not used (nseindia.com and niftyindices.com refuse the sandbox); a straddle-implied ~30-day vol stood in and showed no usable gate either. |
| Caveat | The gate the bean implements sums volume over the **cached legs** within ±200 of the session-open ATM. The replica summed every 50-point strike in that window; the leg-level version (the traded option alone against its own history) was tried and **hurt** (hold +3,676 vs +6,366) because a strike's own history is dominated by the days it was not near ATM. A depth-0 config therefore does not run the gate the study measured. Walk-forward: rules chosen on one year improved the other year's average in 5 of 6 tests but lowered total points in 5 of 6 — the gates are quality filters. |
| Impact | **Measured 2026-09-06 — engine cross-run over 2024 on the [S31](#s31-first-engine-replay-of-strategy8-2024-depth-2-ladder-2715-pts--770-trades--but-the-leg-set-is-not-the-replays) config set** (`strategyIds=9&configStrategyId=8`, depth 2, seeds 2.00 / 0.250 / 2, 6,225 ticks, 0 failures, 191 s). Strategy 9: **298 trades, +1,821.4 pts, +6.11/trade, 52.7% win, PF 1.468, max DD −238**, against Strategy 8 on the identical configs: 770 / +2,715.1 / +3.53 / 42.9% / 1.202 / −818. Same shape as the replica (602 vs 1,780 trades, +4.8 vs +2.0, PF 1.38 vs 1.16, DD −265 vs −727): the gates give up a third of the total points for per-trade quality and cut the drawdown by two thirds. Exits: `FORCE_CLOSE` 162 @ +30.22, `TRAIL_SL` 98 @ −13.60, `STOP_LOSS` 38 @ −45.82. **Not a filtered subset of the Strategy 8 ledger:** only 119 of the 298 are exact twins (same config, leg, entry bar) of a Strategy 8 trade; the other 179 are entries Strategy 8 never took because its one slot was already occupied — the gates free slots as much as they filter, so read the two ledgers as different trade sequences on the same signals. On the 119 shared entries Strategy 8 averaged +8.81, against +2.56 on the 651 it took that Strategy 9 refused. Gate check from the ledger: all 298 entries have DTE ≥ 2 (min 2) and signal-candle close position ≥ 0.25 (min 0.251), where Strategy 8's 770 fail 318 and 411 of the same tests — those two gates held on every entry; the volume gate cannot be re-derived from the ledger and is verified only by the `[tick]` DEBUG `legs=` line. Still open: the trade-by-trade diff against the replica's LG sheets. |
| Fix sketch | If the engine ledger reproduces the replica, consider the V2 (spot-signal) form — `day move already in favour` was the one gate that added points there (+856) — as its own strategy on the underlying series; not built. |
| Effort | **M** for the measurement. |
| Priority | _Open — filed 2026-09-06._ |

---

### S29. `Strategy8` is the intraday form of the "20SMA 15min candle" rule — the replay's edge was in carrying to expiry

| | |
|---|---|
| Where | [`Strategy8`](../src/main/java/com/moneymaker/strategy/Strategy8.java), [`RuleEngine.decideWithoutCrossGate`](../src/main/java/com/moneymaker/strategy/rules/RuleEngine.java), the chandelier trail in [`PositionService`](../src/main/java/com/moneymaker/position/service/PositionService.java) (changeset 048). Filed 2026-09-06 with the strategy. |
| Why | The rule (15-min SMA-20 slope down + close below previous close, sell the ATM option, chandelier 2 × ATR-14 stop, no target) was replayed on the dbeaver export Jan-2024 → Dec-2025 in Python before it was coded here. **Measured:** intraday (square-off 15:15) 1,780 trades, +2.0 pts/trade, +3,532 pts, PF 1.16, 20/24 months positive; held to the weekly expiry 1,060 trades, +6.0/trade, +6,366 pts, PF 1.43, max DD −423, 22/24 months positive. The always-in baseline with the same exit and no signal made +5,518 pts held to expiry (PF 1.19, DD −1,008): the entry rule adds ~850 pts and halves the trade count and drawdown; the profit is theta. The bean implements the intraday form because every ledger invariant (15:15 close signal, 15:20 force-close, per-day caps and loss cap, backtest day loop) assumes flat overnight. |
| Differences from the replica | Ledger fills a trailed stop at the floor even when the bar gapped through it (replica: at the open); `max_sl_points = 60` on the standing rule caps the first stop below 30% on premiums above 200 (replica: pure 30%); the replica traded the ATM strike on every bar while a tagged `AUTO_DOWNTREND` config offers the detector's legs and only when it wrote a 15-minute row; ATR is a simple mean, not Wilder, in both. Costs: replica 1 pt/round trip; here `charge_rate`. |
| Impact | **Partially measured 2026-09-06 — see [S31](#s31-first-engine-replay-of-strategy8-2024-depth-2-ladder-2715-pts--770-trades--but-the-leg-set-is-not-the-replays)** (2024, depth-2 ladder: 770 trades, +3.53/trade, +2,715 pts, PF 1.202, DD -818; the whole edge is the 15:15 close exit). That run is not the depth-0 single-ATM-leg setup this entry asks for, so the trade-by-trade diff below is still open. The Python numbers are not an engine replay. Measure by running a `8`-tagged month (or a hand-made 15-minute depth-0 config) through the backtest and diffing the ledger against the replica's `LG V1 chand 2xATR intraday` sheet trade by trade, as was done for strategies 6/7. |
| Fix sketch | Hold-to-expiry needs: (a) `Strategy8.buyRulesFor` to fire the close-time BUY only on the leg's expiry day; (b) `OrderService.forceCloseOpenPositions` to skip strategy-8 orders on non-expiry days; (c) the backtest day loop and `BacktestingPositionMonitorService` cache to keep quoting an order opened on an earlier day; (d) per-day caps to count carried positions as open; (e) overnight margin. Behind a `strategy_defaults.carry_to_expiry` flag so nothing changes for 1–7. Do not build it without the paired before/after replay (Rule 0(c)). |
| Effort | **M** for the measurement. **L** for the carry-over. |
| Priority | _Open — filed 2026-09-06. Measure first: if the engine's intraday ledger does not reproduce the replica's +2.0/trade, the carry-over is moot._ |

---

### S26. Pressure's trigger fires close to, but not identically with, the reference implementation

| | |
|---|---|
| Where | [`PressureScore`](../src/main/java/com/moneymaker/strategy/pressure/PressureScore.java) + [`SpotFeatures`](../src/main/java/com/moneymaker/indicator/series/SpotFeatures.java). Filed 2026-09-05 from a user cross-check of a local run against the reference ("dbeaver Pressure") over **1–23 Jan 2024**. |
| Why | The user compared spot entries one for one. **Of 78 local spot entries, 65 had a reference entry within 15 minutes and 63 of those matched direction** — the user's own reading was "the same strategy with a slightly different trigger". The clearest divergence is the very first ticket of the window: the local build **SELL at 09:35 on 1 Jan** (a −50 loser) against the reference **BUY at 09:40**, an opposite-direction call on the first bars of the run. From 2 Jan 09:55 onward the two mostly lock, including an identical 21636.80 SELL. That pattern — disagreement concentrated at the very start, agreement thereafter — points at **indicator warm-up** rather than at the scoring rules: the first replayed day has the least history behind RSI, ATR/Supertrend and ADX, and `Supertrend.direction` additionally seeds `UP` at its first computable bar, so an early bar can be scored off a direction that is a seed rather than a measurement. [S22](#s22-pressures-session-vwap-is-weighted-by-option-volume-because-nifty-spot-has-none) (the option-volume VWAP proxy) is the other candidate and the two are not separable without measurement. |
| Caveat on the source data | **The compared run came from a stale JVM** carrying the strike-collapse defect (three books trading one leg) and no per-day VWAP weights cache. That defect is in leg *selection*, not in scoring, so the **spot** entry timestamps the user matched on should be unaffected — but this entry should be re-derived against a clean run before any conclusion is drawn from it. The 78-vs-87 spot ticket count is itself part of what needs re-measuring. |
| Impact | **Unquantified.** 63 of 78 directionally matched inside 15 minutes is agreement, not equivalence, and the residual is where the P&L difference lives (user's slice: local spot +6.5 mean pts / 65.4% WR against reference +8.5 / 71.3%). The measurement: on a clean run over the same window, join local spot entries to reference entries on nearest timestamp, and bucket the unmatched ones by which score term differed. That names the cause instead of guessing between warm-up and VWAP. |
| Fix sketch | Nothing to change yet, and **nothing to change without the user** — this is the entry trigger. If warm-up proves to be the cause the candidate is to require a minimum number of warm bars before Strategy 5 will score at all (today it scores as soon as each indicator is individually non-NA, and `Supertrend` reports a seeded direction rather than `UNKNOWN` on its first computable bar). That is a real behaviour change: it suppresses early-session entries, so it needs a paired before/after on the same window per working rule 3. Widening the warm-up window is the cheaper half and changes no rule. |
| Effort | **S** to re-measure on a clean run. **M** if a warm-bar minimum is introduced, since that is a new `TradeConfig` field under Rule 9 plus a paired run. |
| Priority | _Open — filed 2026-09-05. Worth re-deriving as soon as a clean full-year run exists; the January slice is the natural window because a reference comparison already exists for it._ |

---

### S21. `Strategy6`'s three gates are replay-selected — the constants are strategy identity, not config

| | |
|---|---|
| Where | [`Strategy6`](../src/main/java/com/moneymaker/strategy/Strategy6.java) (built 2026-09-05 on the user's request: *"Build this as strategy 6"*), [`CommonRules.higherTimeframeSmaDownTrending`](../src/main/java/com/moneymaker/strategy/rules/CommonRules.java) / `isAtOrBeforeEntryCutoff`, the stop-loss lock gate 6 in [`OrderService.handleSignal`](../src/main/java/com/moneymaker/order/service/OrderService.java), and the confirmation-timeframe fetch in [`AnalysisScheduler`](../src/main/java/com/moneymaker/scheduler/AnalysisScheduler.java). |
| Why | The gates come from a Python replica of this pipeline (detector + strategies 1/2 + `OrderService` caps + the current `PositionService` bracket, entries verified bar-for-bar against `analysis/s6-measurement/run-B-ledger.csv`) replayed over the dbviewer NIFTY 5-minute series, Dec-2023 → Dec-2025, `max_deviation = 5`, the standing AUTO_DOWNTREND numbers, lot 75, changeset-029 charge rates. Baseline: strategy 1 1,581 trades, +695 pts/share, PF 1.03, ≈ ₹+5k after charges; strategy 2 963 trades, +102 pts, PF 1.01, ≈ ₹−22k. What lost: re-entries after a stop in the same book (343 trades, −583 pts), entries after 15:00 (86 trades, −304 pts, 29% win), and signals while the leg's 15-minute SMA-50 was not in a whole-day down-trend (725 trades, −925 pts vs +1,620 when it was). Replayed end-to-end (slot and cap effects included), strategy 2 + the three gates with the 15-minute rule reading *unknown ⇒ allow*: **561 trades, +2,021 pts, PF 1.28, 67% win, ≈ ₹+134k**, 2024 +907 / 2025 +949, positive in every half-year, max drawdown ≈ ₹28k. Strategy 1 + the same gates: 769 trades, +1,826 pts, PF 1.17. Gating the 15-minute rule as *unknown ⇒ block* (which removes the 09:15–09:25 entries, the replay's best slice at +1,143 pts over 199 trades) cost ≈ 200 pts. The 20-SMA slope filter alone is noise — strategy 1's slope-up entries (840) and slope-not-up entries (741) average the same — but on top of the other three it lifted PF 1.17 → 1.28 and smoothed the curve, which is why 6 extends 2 rather than 1. Full variant grid and ledgers are in the user's `money_maker_backtest_analysis.xlsx` (2026-09-05). |
| Impact | **Measured in replay only, and in-sample:** roughly thirty variants were tried on the same two years and these are the survivors, so the live numbers will be smaller. The rank-on-2024 / read-2025 check held for the three gates (2024 +1,067 → 2025 +693 for strategy 1 + gates) and failed for the runner-ups (no-re-entry alone +1,461 → +119; skip-Fridays +1,050 → −38). 2025H2 was the weakest half for every variant. Two further caveats on the money figures: the charge rates are the unverified changeset-029 seeds, and lot 75 is applied throughout although NIFTY traded 25-lots for most of 2024. |
| Open questions | **(a) Constants vs columns.** The 15-minute width, SMA-50 and the 30-minute cut-off are constants on the bean, and the stop-loss lock is a bean declaration, not `TradeConfig` columns — CLAUDE.md #9 says tunables belong in config, and #9 also says to ask before hardcoding, which this build did by being asked to build exactly this. Promote them the day a second strategy wants different values (three columns + `TradeConfigFormDTO` / `TradeConfigViewDTO` / both directions of `TradeConfigAdminService` / the combined query, per the four-places note in ORDERS_AND_POSITIONS.md). **(b) The replica's 15-minute series was always present; here it is fetched from the 15-minute underlying bar's ATM window,** so at a tick where the 15-minute and 5-minute ATM differ a leg can lack its confirmation series and read *unknown*. Rare (the two closes differ by less than a strike almost always) and it errs toward allowing — measure with a replay whose `[tick]` log counts `htf15Sma50DownOrUnknown` unknowns versus verdicts. **(c) Live parity of the confirmation fetch:** under `BROKER` the extra 15-minute request per leg per tick is one more rate-limited call; `BacktestMarketDataCache` makes it free in replay. Watch `kiteHistorical` throttling on the first live day with a 6-tagged config. **(d) The 09:15-bar edge** (+1,143 pts over 199 trades in the baseline) is the largest single slice in the replay and is not a gate here — it rides on *unknown ⇒ allow*. Whether it deserves its own strategy (opening-bar entries only: 220 trades, PF 1.37) is unmeasured out of sample. |
| Measured after the build (2026-09-05, same replica) | **Config knobs on top of 6 all lose:** `no_of_trades = 2` → +1,882 pts (from +2,021), 5-minute rules only → +1,071, SMA-200 dropped → +1,889, `min_option_price = 100` → +1,567, all four → +740. The gates already remove what those knobs remove. **Bracket:** `target_pct`/`sl_pct` 0.25/0.25 → +1,856; `max_sl_points` 45 → +1,584. **Close signal 15:00** → +2,069 (a wash). **Detector `max_deviation`** 0 / 2 / 5 → PF 1.29 / 1.30 / 1.28 — robust to the config-generation setting. Two small gates helped and were **not** adopted: blocking CE sells while spot RSI-14 < 30 (+2,207, PF 1.31, but it is 12 trades over two years) and skipping Fridays (+1,786 with a smaller drawdown but 2025 halves). Strategy 1 with the same three gates and no slope filter: +1,826 / PF 1.17, and +1,756 / PF 1.18 with `no_of_trades = 2` — a slope-less variant is not worth a class. Changeset 046 therefore seeds strategy 6's `strategy_defaults` as a copy of strategy 1's block so nothing is left to be typed differently. |
| Fix sketch | Nothing to fix in the build. Per working rule 3, before changing any of the three gates: tag one window's configs `2,6`, replay, and diff the two ledgers row by row — same detection by construction, so every difference is one of the gates, and the `[tick]` log names which. |
| Effort | **S** for the paired replay; **M** to promote the constants to columns (four places plus the query contract test). |
| Priority | _Open — filed 2026-09-05 at build time. Not a defect; a record of what was measured and what was not._ |

---

### S22. `Strategy7`'s first-hour gate is the one regime signal that transferred — gap rule and regime model did not

| | |
|---|---|
| Where | [`Strategy7`](../src/main/java/com/moneymaker/strategy/Strategy7.java) (built 2026-09-05 on the user's request: *"Create this as strategy 7"*), [`CommonRules.firstHourMoveInFavourAtr`](../src/main/java/com/moneymaker/strategy/rules/CommonRules.java) / `sessionAtr`, changeset 047. |
| Why | The user's regime thread ("Nifty trading indicator analysis") built a 3-class sideways/rally/decline model (AUC 0.65 at 10:15, 0.58 at 09:20), found the gap rule (\|open − prev close\| ≤ 0.5 ATR) to be its one robust sideways identifier, and used the 09:15 ATM straddle as the expected move. Each was tested here against the Strategy 1 and Strategy 6 ledgers, then the promising ones as real gates. **Diagnosis first:** by realised day type, Strategy 6 makes +1,866 pts on sideways days (367 trades, PF 1.45), +2,036 on trends in the leg's favour (110, PF 3.93) and loses −1,881 on trends against (84, PF 0.23) — every loss is a trend-against day, so the only useful regime signal is one that flags those in time. **First-hour direction** (entries after 10:15; skip when the first hour moved against the leg by > 0.2 × expected move): Strategy 6 561 → 518 trades, +2,021 → +2,225 pts, PF 1.28 → 1.34, all half-years positive; the 43 removed trades were trend-against days 35% vs 14% base. Robust: thresholds −0.1/−0.2/−0.3 → +2,186/+2,225/+2,046; ATR-normalised −0.1/−0.2/−0.3 → +2,239/+2,125/+2,114. Built as ATR-based −0.2 (no straddle fetch needed): **525 trades, +2,125, PF 1.31, ≈ ₹143k**, 2024 +814 / 2025 +1,204, halves +545/+269/+815/+389, max DD ≈ ₹29.5k. Strategy 1 with the gate alone: +695 → +1,401. **Gap rule** (skip \|gap\| > 0.5 ATR days): +2,086 but 2025H2 flat — half of this strategy's trend days are the favourable, gap-and-go kind, so the rule throws away 09:15 winners with the losers. **Yesterday's type** (close within the day's range, body ≥ 0.3): PF 1.28 → 1.37 but 1,664 pts on 374 trades — a variance trade-off, not adopted. **Expected-move / IV level, range-so-far, position in the day's range:** no monotonic signal (IV 1.2–1.5× ATR loses in both ledgers but the buckets either side win). **The regime model itself:** not ported — the source thread found it weak for premium selling (its "sideways" is really "low IV"), and this strategy's edge is the option's own premium trend, which the model does not see. |
| Impact | In-sample on 2024–25 like S21; the gate's contribution is +5–10% and the threshold plateau is the evidence it is not a fit. Definition parity with the replay: move = close of the last bar before 10:15 − open of the 09:15 bar; ATR-14 = mean true range of the 14 completed sessions before the day, rolled up from the cached intraday series. The replay's ATR came from daily bars including the 15:30 print, which the intraday roll-up may lack under `BROKER` if the provider drops it — a few points of range, immaterial at a 0.2-ATR threshold. |
| Open questions | **(a)** The checkpoint (10:15) and threshold (0.20 ATR) are strategy constants — S21 (a) applies verbatim. **(b)** Under `BROKER`, does the cached underlying series carry enough sessions for the ATR (the 35-day lookback gives ~24; the rule needs 5) on the first live day after a restart? Unknown ⇒ allow covers the failure mode, but count `firstHourNotAgainstOrUnknown` unknowns in the `[tick]` log on day one. **(c)** The gate reads the underlying at the *signal's* interval; for a 15-minute signal the 09:15 bucket's open is the session open, as required — verify once against the 5-minute series in a replay diff of a `6,7`-tagged window. |
| Fix sketch | Nothing to fix. Per working rule 3: tag one window's configs `6,7`, replay, diff the two ledgers — same detection and same five other rules by construction, so every difference is this gate. |
| Effort | **S**. |
| Priority | _Open — filed 2026-09-05 at build time._ |

---

## Resolved

### S2. `Strategy1` scanned every config's legs, not its own — **RESOLVED 2026-08-25**

*Moved here from `GAPS.md` #19 on 2026-08-30 when this file was split out.*

| | |
|---|---|
| Where | `keyMatches`, now on [`AbstractSmaCrossStrategy`](../src/main/java/com/moneymaker/strategy/AbstractSmaCrossStrategy.java) (it lived on `Strategy1` until the base class was extracted on 2026-08-30), reading [`SharedData.strikeMarketDataByInstrumentAndInterval`](../src/main/java/com/moneymaker/shared/data/SharedData.java) written by [`AnalysisScheduler.toStrikeMarketDataKey`](../src/main/java/com/moneymaker/scheduler/AnalysisScheduler.java) |
| Why | The writer keys each entry `instrumentToken\|interval\|optionType\|strike\|optionToken\|itmDepth\|otmDepth` and contributes only the legs of the config that fetched them. The reader matched an `instrumentToken\|interval\|` **prefix** only — `optionType` and both depths were never compared. `trading_side` reached the strategy solely as the sort direction (`strikeComparator(isCe)`), never as a filter, so every config scanned the union of all configs' legs. |
| Impact | On any day with the normal CE + PE config pair, each signal fired once under each config id and the ledger recorded **every trade twice**, with half the rows carrying an option type contradicting their own config's `trading_side` (e.g. a PE trade booked under a CE config). Realised P&L over such a run is doubled. The `existsByTradeConfigIdAndOptionTokenAndEntryDirectionAndEntryTime` dedupe guard could not catch it — it keys on `tradeConfigId`, so the pairs are legitimately distinct rows. Where several legs fired on one tick the two configs landed on *different* strikes instead of identical ones, because they sort in opposite directions and `no_of_parrellel_trades` cut the scan short at opposite ends — which is why the duplication was not uniform and read as "sometimes the wrong strike". |
| Resolution | `keyMatches` now splits the key and compares `optionType` against the config's resolved `trading_side` plus both depth segments against the config's own. `isCallSide` became `resolveOptionType`, mirroring `AnalysisScheduler.resolveOptionType` including its null-on-unresolved behaviour — the writer skips the fetch for an unresolved side, so defaulting to CE would have made such a config scan someone else's legs. |
| Related | Sibling of [`GAPS.md` #18](GAPS.md#18-shareddataoptiontokenmap-was-keyed-by-strike-alone--resolved-2026-08-22) — same root shape (a shared cache whose key was less specific than its contents), different map. Also worth re-reading alongside the `(tradeConfigId, strategyId)` ledger identity introduced by changeset 031: two strategies now legitimately share a config and each hold their own position on the same leg, which is the *intended* version of what this bug did accidentally. |
| Effort | **S** |

### S3. Stale-bar guard (`asOf`) — **RESOLVED 2026-08-30**

| | |
|---|---|
| Where | [`Strategy.execute(TradeConfigCombinedDTO, LocalDateTime asOf)`](../src/main/java/com/moneymaker/strategy/Strategy.java) → [`StrategyFactory.execute`](../src/main/java/com/moneymaker/strategy/StrategyFactory.java) → [`AbstractSmaCrossStrategy.execute`](../src/main/java/com/moneymaker/strategy/AbstractSmaCrossStrategy.java); tick supplied by [`AnalysisScheduler.runStrategies(asOf)`](../src/main/java/com/moneymaker/scheduler/AnalysisScheduler.java) from [`BacktestAnalysisService`](../src/main/java/com/moneymaker/backtesting/BacktestAnalysisService.java) |
| Why | Per the interface Javadoc: a strategy must not act on a bar belonging to an earlier session than `asOf`. The candle series spans the whole SMA lookback and ends at the newest bar **settled** by `asOf`, so for a coarse timeframe that bar is still the previous session's close until the day's first bucket completes — on a 15-minute series, until 09:30. |
| Impact | **Quantified, not hypothetical.** In the 2024 replay (88 ledger rows) it produced **13 exits stamped before their own entry** — `exit_price` matching the previous session's last 15-minute bar close exactly — and **2 entries** stamped on the previous session's 15:15 bar, which then read as overnight holds in an intraday system and were closed the next day by the position monitor. Every affected entry fell on the day's first two ticks (09:15 / 09:20), and every affected config carried a 15-minute timeframe. `Strategy2`'s SMA-20 slope rule was comparing two of yesterday's bars in the same window — see [S1](#s1-strategy2s-sma-20-slope-filter-is-inert-when-the-slope-is-unknown--parked-2026-08-30). |
| Decision (Rule 0) | **Skip the leg for this tick.** The alternative — walking back to the newest in-session bar — was rejected: on the day's first ticks there is no in-session bar for a coarse timeframe to walk back to, so it would either find nothing or silently substitute a finer timeframe's bar and misreport which timeframe fired. "This timeframe has no settled bar yet today" is the honest answer. Signed off with the two consequences stated explicitly: a 15-minute timeframe emits no signal before 09:30, and 15-minute trades continue to exit via end-of-day force-close rather than the 15:15 close signal (unchanged — bucket 15:15 settles at 15:30, after the backtest day loop's 15:20 cutoff). |
| Resolution | `AbstractSmaCrossStrategy` skips any leg whose newest settled bar pre-dates `asOf`'s date, logging the reason on the `[tick]` line. Only the decision is suppressed — the bars behind it still feed the SMA. [`CommonRules.isMarketCloseTime`](../src/main/java/com/moneymaker/strategy/rules/CommonRules.java) additionally asserts the candle belongs to `asOf`'s session (via a new nullable `asOf` on [`RuleContext`](../src/main/java/com/moneymaker/strategy/rules/RuleContext.java), old constructor retained); that is redundant behind the guard but the predicate was wrong standalone. Documented in [STRATEGIES.md](STRATEGIES.md#the-shared-engine). |
| Related | Landed alongside a second, independent defect in the same replay: monitor-driven exits were priced off whichever cached interval hashed first, including a 10-minute series **no config asks for**. Fixed by `SharedData.latestCachedCandle` (finest cached interval) plus scoping the `AnalysisScheduler` fetch to the config's own timeframes. Both fixes change the ledger by design — see the re-baseline note in [BACKTEST_PERFORMANCE.md](BACKTEST_PERFORMANCE.md#parity-verification-checklist). |
| Effort | **S** |

### S5. Session-window constants are hardcoded while `app.market.*` already exists — **RESOLVED 2026-08-31**

| | |
|---|---|
| Where (as filed) | [`CommonRules.isMarketCloseTime`](../src/main/java/com/moneymaker/strategy/rules/CommonRules.java) — `LocalTime.of(15, 15)`; [`BacktestAnalysisService`](../src/main/java/com/moneymaker/backtesting/BacktestAnalysisService.java) — `marketStart = 09:20`, `marketEnd = 15:20`. The codebase held two answers to "when does the session end": these constants, and `app.market.*` / [`MarketHoursService`](../src/main/java/com/moneymaker/market/service/MarketHoursService.java), the declared source of truth. |
| Resolution | All three times now derive from `MarketHoursService`: `closeSignalTime()` = `app.market.close` − `app.market.close-signal-offset-minutes` (default 15 → 15:15), `replayFirstTick()` / `replayLastTick()` = open + / close − the two `app.market.replay-*-offset-minutes` keys (defaults 5 / 10 → 09:20 / 15:20). The close-signal time rides into rules on a new nullable `RuleContext.closeSignalTime` (populated by `AbstractSmaCrossStrategy` from an optional injection; null degrades to the legacy 15:15 — the same pattern `asOf` used). Startup validates the derived window (an empty replay window or an out-of-session close signal fails boot). Defaults are pinned by `MarketHoursServiceDerivedTimesTest`, so a default drifting from its legacy constant is a test failure, not a silent retune. |
| Measurement (the sign-off condition) | Paired run on 2024-01-01 → 2024-01-31, identical config state (pct bracket live, ladder seeded), tables wiped between arms, one variable — the S5 code. **Arm Before** (constants, post-GAPS #4 build): 21 trades, −127.40/share, exits 5 SIGNAL / 7 TARGET / 5 STOP_LOSS / 4 TRAIL_SL. **Arm After** (derived times): 21 trades, −127.40, **per-order diff empty (identical modulo autoincrement id)**. Unification confirmed as *not* a behaviour change. Arm Before also reproduced the S6 Arm B ledger byte-identically — the [S11](#s11-wall-clock-scheduler-threads-mutate-replay-state-mid-run) part-(b) verdict. Artifacts: `analysis/s5-measurement/`. |
| Still true after the change | The 15:20-vs-15:30 asymmetry (a 15-minute bucket stamped 15:15 never settles in backtest, so 15-minute trades exit via force-close) is now *visible and configurable* rather than hardcoded, but the default behaviour is unchanged by design. Retuning any of the three offsets is a fresh Rule 0 change with its own pair. |
| Effort | **S** code as predicted; the ledger diff ran twice in ~36 s each on the cached pipeline. |

### S7. Signal flip-flop consumes the daily trade cap — **RESOLVED 2026-08-31**

| | |
|---|---|
| What it was | The 2024-01-02…04 run of 2026-08-30 (18 trades) showed every exit as `SIGNAL` with near-immediate same-leg round trips consuming the `numberOfTradesPerDay = 5` cap. |
| Resolution | **Closed by the user 2026-08-31 with the measurement showing zero occurrences.** On the full Jan-2024 window, post-S3 / post-`latestCachedCandle`, both arms of the S4 pair had **0** same-leg re-entries within one candle interval (0/12 and 0/16), the busiest config-day used 3 of 5 cap slots, and the exit mix is no longer SIGNAL-only. The original observation predates the S3 stale-bar guard, so the two ledgers were never comparable — the flip-flop was in all likelihood a symptom of the stale-bar defect S3 fixed, not an independent behaviour. No lever was ever built; nothing to revert. Re-open if a post-fix window ever shows the cap consumed by round trips again. |
| Effort spent | Measurement only, piggybacked on the S4 pair at no extra cost. |

---

## Not tracked here

- **`stratergy_id` column-name typo** — schema naming, not strategy behaviour.
  Stays at [`GAPS.md` #10](GAPS.md#10-tradeconfigstratergyid--column-name-typo).
- **Indicator correctness** (SMA source = candle low, EMA/RSI stubs) — those are
  `indicator/` concerns. `GAPS.md` #15 covers the stubs; the deliberate
  low-vs-close choice is documented in `SMAIndicatorImpl`'s Javadoc.
- **How a config reaches a strategy** (the `trade_config.strategy_ids` fan-out from
  changeset 031) — that is routing, described in
  [STRATEGIES.md](STRATEGIES.md). An entry belongs here only when it changes
  which *signals* a strategy emits.
